# Project Completion Roadmap

How to use this document: work top to bottom. Only `[ ]` rows are candidates; `[x]` rows satisfy
`deps:`. This is a **living backlog** — completed items are archived to
[`roadmap-history.md`](./roadmap-history.md), so a `deps:` entry naming no current row means
_already shipped_, not an error. Resolve any such reference there.

**Phases 1, 2, 3 and 4 are complete and archived** to
[`roadmap-history.md`](./roadmap-history.md) — resolve any `deps:` naming a row you cannot find here
against that file. The rows under "Deferred / human-gated" remain invisible to selection.

**The registries are the default frontier; a phase here is an override.** When no phase holds an
actionable row, `select-next.mjs` falls through to [`docs/bugs/bug-registry.md`](../bugs/bug-registry.md)
and [`docs/tasks/task-registry.md`](../tasks/task-registry.md) directly, so a filed bug or task is
visible to `/develop-next` **without anyone transcribing a row here**. A task is in that frontier at
`draft` or later (widened by T71 to equal what `develop-task` accepts); a bug at `new` or `reopened`.
For a registry row, **the document's frontmatter decides, not the row** — a stale row cannot resurrect
finished work. Rules:
[`roadmap-selection.md`](../../skills/develop-next/references/roadmap-selection.md)
§"Registry fallback frontier".

Add a phase only to express *sequencing* the registries cannot: a deliberate order, a dependency
chain, a human gate. **Phase 5 below is such an override** — it states the current frontier's order
explicitly rather than leaving it implied by priority ranking.

> ⚠️ **A phase costs upkeep that a registry row does not, and the cost is not obvious.** Roadmap rows
> are matched on the **checkbox alone** — they do **not** consult the target document's frontmatter.
> An unticked `[ ]` row whose task is already `accepted` is still selected, and `/develop-task` then
> HALTs on it ("already past development"), stalling an unattended loop on finished work. Verified
> against T71 while Phase 5 was authored. **So: tick each row `[x]` the moment its task is accepted.**
> The registry needs no such discipline, which is why a phase should exist only while its ordering is
> actually doing work.

Selection is executed by `select-next.mjs`, never by eye:

```bash
node .agents/skills/develop-next/scripts/select-next.mjs --lint      # format check
node .agents/skills/develop-next/scripts/select-next.mjs             # what's next
node .agents/skills/develop-next/scripts/select-next.mjs --batch     # what N agents can do at once
```

Marker vocabulary and the full algorithm: `skills/develop-next/references/roadmap-selection.md`.

## Legend — conflict footprint (`touches:` tags)

Read only by `--batch`. Two rows conflict when they share a tag that **either** side marks `!`
(hard/exclusive); shared `~` (soft) tags are tolerated on a second-merger-rebases basis.

| Tag                | Covers                                                                 |
| ------------------ | ---------------------------------------------------------------------- |
| `jira-sync`        | `shared/resources/jira-sync.js`, `jira-stage.js`                       |
| `gh-stage`         | `shared/resources/gh-stage.js`                                         |
| `workflow-engine`  | `shared/resources/tracker-workflow.js`, `yaml-subset.js`               |
| `pipeline-steps`   | `shared/resources/develop-pipeline-step-*.md`, `lite-mode.md`          |
| `orchestrators`    | `skills/develop-next/SKILL.md`, `skills/develop-batch/SKILL.md`        |
| `setup-consumer`   | `scripts/setup-consumer.sh`                                            |
| `docs-config`      | `docs/reference/configuration.md`, `docs/reference/tracker-workflow.md` |
| `bundles`          | `skills/*/references/*` (regenerated by `npm run bundle`)               |
| `change-log`       | `shared/resources/change-log.js`, `document-change-log.md`              |
| `templates`        | `skills/*/resources/*template*`, `docs/templates/*`, `create-*/SKILL.md` |
| `review-skills`    | `skills/review-*/SKILL.md`, `skills/edit-*/SKILL.md`                    |
| `loop-supervisor`  | `skills/loop-supervisor/*`, `evals/loop-supervisor/*`                  |
| `test-harness`     | `package.json` test script, `shared/resources/tests/*`                  |
| `qa-skills`        | `skills/qa-story/SKILL.md`, `skills/qa-task/SKILL.md`                   |
| `selection`        | `skills/develop-next/scripts/select-next.mjs`, `references/roadmap-selection.md` |
| `finalise`         | `skills/finalise/SKILL.md`, `shared/resources/finalise-dod-*-prompt.md`  |
| `mutation-proving` | `shared/resources/mutation-proving.md`                                  |
| `docs-pipeline`    | `docs/runbooks/*`, `docs/concepts/*`, `docs/reference/pipeline-artifacts.md`, `docs/operations/workflows.md`, `skills/develop-*/README.md` |
| `pipeline-lock`    | `shared/resources/advance-pipeline-lock.sh`, `advance-pipeline-lock.test.sh`  |

---

## PHASE 5 — Current frontier

Stated in order so the sequence is explicit in one place rather than inferred from priority ranking.
This phase reproduces what the registry fallback already selects; it does not reorder it.

**Tick each row `[x]` as its task is accepted** — see the warning above; an unticked row for an
accepted task stalls the loop.

- [x] **T67** Make QA execute a prose skill, not only read it · deps: none · touches: pipeline-steps!, qa-skills!, test-harness~ · /develop-task docs/tasks/task.67.execute-the-skill-qa-gate/task.67.execute-the-skill-qa-gate.md — ✅ **accepted + merged** ([PR #289](https://github.com/Gamaroff/agent-skills/pull/289), QA PASS 90/100)
- [x] **B3** Three CLIs truncate piped stdout at ~64KB by exiting before the write drains · deps: none · touches: selection!, test-harness! · /develop-bug docs/bugs/bug.3.stdout-truncation-on-exit/bug.3.stdout-truncation-on-exit.md — ✅ **accepted + merged** ([PR #290](https://github.com/Gamaroff/agent-skills/pull/290), DoD 9/9 PASS)
- [x] **T75** Make the pipeline quality gate run what CI runs · deps: none · touches: orchestrators!, pipeline-steps!, test-harness!, bundles~ · /develop-task docs/tasks/task.75.quality-gate-matches-ci/task.75.quality-gate-matches-ci.md — ✅ **accepted + merged** ([PR #291](https://github.com/Gamaroff/agent-skills/pull/291), QA PASS 100/100)
- [x] **B4** Snippet engine silently no-ops when invoked through a symlinked path · deps: none · touches: qa-skills!, test-harness! · /develop-bug docs/bugs/bug.4.snippet-engine-symlink-noop/bug.4.snippet-engine-symlink-noop.md — ✅ **accepted + merged** ([PR #292](https://github.com/Gamaroff/agent-skills/pull/292), DoD PASS, CI 4/4)
- [x] **B5** access-config parity JS probe records a timeout as a real answer · deps: none · touches: test-harness! · /develop-bug docs/bugs/bug.5.access-parity-js-probe-conflates-timeout/bug.5.access-parity-js-probe-conflates-timeout.md — ✅ **accepted + merged** ([PR #293](https://github.com/Gamaroff/agent-skills/pull/293), DoD 6/6 PASS, CI 4/4)
- [x] **T68** `/review-code` branches on TRACKER where it should branch on VCS · deps: none · touches: review-skills!, test-harness~ · /develop-task docs/tasks/task.68.review-code-vcs-branch/task.68.review-code-vcs-branch.md — ✅ **accepted + merged** ([PR #294](https://github.com/Gamaroff/agent-skills/pull/294), DoD 6/6 PASS, QA PASS 100/100, CI 4/4)
- [x] **T69** Give `/qa-story` and `/qa-task` a Bitbucket PR-comment path · deps: none · touches: qa-skills!, test-harness~ · /develop-task docs/tasks/task.69.qa-bitbucket-pr-comment/task.69.qa-bitbucket-pr-comment.md — ✅ **accepted + merged** ([PR #295](https://github.com/Gamaroff/agent-skills/pull/295), DoD PASS, QA PASS 100/100, CI 4/4)
- [x] **T72** Pin the bug-axis divergence exactly instead of asserting it loosely · deps: none · touches: selection! · /develop-task docs/tasks/task.72.pin-bug-axis-divergence/task.72.pin-bug-axis-divergence.md — ✅ **accepted + merged** ([PR #296](https://github.com/Gamaroff/agent-skills/pull/296), QA PASS 100/100)
- [x] **T73** Make the DoD security check execute candidate inputs, not grep for them · deps: none · touches: finalise!, bundles~ · /develop-task docs/tasks/task.73.dod-security-probe-not-grep/task.73.dod-security-probe-not-grep.md — ✅ **accepted + merged** ([PR #297](https://github.com/Gamaroff/agent-skills/pull/297), DoD PASS, QA PASS 95/100, CI 4/4) — found 12 open classifier routes on its first run → `bug.6`
- [x] **T74** A security re-review must re-probe, not re-read · deps: none · touches: qa-skills!, bundles~ · /develop-task docs/tasks/task.74.security-re-review-reprobes/task.74.security-re-review-reprobes.md — ✅ **accepted + merged** ([PR #299](https://github.com/Gamaroff/agent-skills/pull/299), DoD PASS, QA PASS 100/100, CI 4/4) — probe mode 12/0 at the DoD gate; found a red-CI dead link and a third stale copy of the scoping rule
- [x] **B9** The registry frontier ignores the `Depends on` column, so it can nominate work whose prerequisite is unbuilt · deps: none · touches: selection!, test-harness~, docs-pipeline~ · /develop-bug docs/bugs/bug.9.registry-frontier-ignores-depends-on/bug.9.registry-frontier-ignores-depends-on.md — ✅ **accepted + merged** ([PR #303](https://github.com/Gamaroff/agent-skills/pull/303))
- [x] **T76** State what a mutation proof does not tell you · deps: none · touches: mutation-proving!, bundles~ · /develop-task docs/tasks/task.76.mutation-proof-limits/task.76.mutation-proof-limits.md — ✅ **accepted + merged** ([PR #304](https://github.com/Gamaroff/agent-skills/pull/304), DoD PASS, QA PASS 100/100, CI 4/4) — review caught the task describing a 96-line/four-shape file that had already grown to 140/five, and stopped a bare "9 proofs held" going into the one document that forbids overclaiming
- [x] **T70** Build the inline PR comment primitive, on GitHub and Bitbucket · deps: none · touches: review-skills!, test-harness~ · /develop-task docs/tasks/task.70.inline-pr-comments/task.70.inline-pr-comments.md — ✅ **accepted + merged** ([PR #308](https://github.com/Gamaroff/agent-skills/pull/308), QA PASS 92/100)
- [x] **T77** Run the PR conformance review before a work item is finalised · deps: none · touches: pipeline-steps!, review-skills!, docs-pipeline!, test-harness~, bundles~ · /develop-task docs/tasks/task.77.review-pr-in-pipeline/task.77.review-pr-in-pipeline.md
- [x] **T89** Lint for prose-matching assertions that claim a relationship but test only co-occurrence · deps: none · touches: test-harness! · /develop-task docs/tasks/task.89.relationship-assertion-lint/task.89.relationship-assertion-lint.md — ✅ **accepted + merged** ([PR #312](https://github.com/Gamaroff/agent-skills/pull/312), DoD PASS, QA PASS 100/100 over 2 cycles, CI 4/4) — found **6 live instances of its own bug class** on its first run
- [x] **T90** `advance-pipeline-lock.sh` reports success for an advance that did not happen · deps: none · touches: pipeline-lock!, bundles! · /develop-task docs/tasks/task.90.pipeline-lock-silent-success/task.90.pipeline-lock-silent-success.md — ✅ **accepted + merged** ([PR #313](https://github.com/Gamaroff/agent-skills/pull/313), DoD PASS, QA PASS 100/100 over 2 cycles, CI 4/4) — QA found a **28 MB corrupted artifact this run had already pushed**, and no gate in the repo could see it
- [x] **T78** Give `develop-bug`'s fix cycle the same fast gate as the other pipelines · deps: none · touches: pipeline-steps!, test-harness~, bundles~ · /develop-task docs/tasks/task.78.develop-bug-fast-gate/task.78.develop-bug-fast-gate.md — ✅ **accepted + merged** ([PR #314](https://github.com/Gamaroff/agent-skills/pull/314), QA PASS 100/100)
- [x] **T83** Skip installing tracker-specific skills the consumer's platform can never fire · deps: none · touches: setup-consumer!, test-harness~, docs-pipeline~ · /develop-task docs/tasks/task.83.platform-aware-skill-exclusion/task.83.platform-aware-skill-exclusion.md — ✅ **accepted + merged** ([PR #315](https://github.com/Gamaroff/agent-skills/pull/315), DoD PASS, QA PASS 95/100 over 3 cycles, CI 3/3) — QA found the filter **inverted** for quoted config, then found the follow-on defect in its own fix
- [x] **T84** Skill install profiles with dependency closure · deps: T83 · touches: setup-consumer!, test-harness~, docs-config~, docs-pipeline~ · /develop-task docs/tasks/task.84.skill-install-profiles/task.84.skill-install-profiles.md — ✅ **accepted + merged** ([PR #318](https://github.com/Gamaroff/agent-skills/pull/318), DoD PASS, QA PASS 95/100 over 4 cycles + /review-pr, CI 4/4) — a consumer now picks `minimal` (5) / `pipeline` (36) / `full` (109); measured **−60%** of the description budget for `pipeline` (35,425 → 14,281 bytes), the tier that is paid on every request before the agent reads an instruction. **The specified design did not work and was replaced mid-implementation.** §3 said to derive the call graph by scraping `/slash-command` tokens from prose; built, measured, and it collapsed `minimal` (5 seeds) and `pipeline` (26 seeds) to the SAME ~34 of 120 skills — a feature that does nothing while reporting success. Prose carries no edge direction: `review-code/SKILL.md:180` literally reads "`/develop-story` and `/develop-task` do **not** call `/review-code`", which the scrape read as two edges. Replaced with a declared `invokes:` frontmatter key on 20 skills; absent key = no edges, the safe default. **27 defects across 4 passes, and every pass found the previous pass's fixes defective** — 11 → 5 → 5 → 10, same author throughout. Four reachable configs silently installed EVERY skill (a typo'd include, an exclude emptying the profile, a comment after `skills:`, a `$`-prefixed profile). The worst was a bare `X=$(cmd)` assignment that aborts the wizard under errexit **introduced in the same commit as a comment warning against it**, invisible to its own test because that test ran under `set +e`. `parseInvokes` needed four fixes before being restructured and given an exhaustive 20-shape matrix rather than patched shape-by-shape. **Two lessons worth carrying.** (1) Tests asserting on SOURCE TEXT caught **0 of 27** defects and twice gave false confidence about the exact line one sat on; one asserted on a code comment, which cannot fail. (2) **Recording a check as unrunnable is a claim** — `shellcheck` was written off across three gates as "not installed, no lane"; docker was available all along and this roadmap's own T83 entry documents it being run that way, and when finally run it found a new SC2155 in this very change. The real `--update` was likewise called impractical, then run non-dry against a full 120-skill install: 120 → 120, nothing pruned, confirming the tracker/profile keep paths by execution. **Residual, deliberate**: `install_skills` still has no behavioural CI coverage — the `--update` proof is manual. Extracting the per-skill decision into a testable helper touches the only code that can delete a consumer's skills and belongs in its own task; `gate.4` records maintainability as CONCERNS for it. Merge authorised by maintainer instruction; no review was recorded on the PR and the DoD says so rather than ticking that box
- [x] **T91** Reconcile install-time and run-time tracker resolution · deps: T83 · touches: setup-consumer!, platform-detection!, test-harness~, docs-config~ · /develop-task docs/tasks/task.91.reconcile-tracker-resolution/task.91.reconcile-tracker-resolution.md — ✅ **accepted + merged** ([PR #320](https://github.com/Gamaroff/agent-skills/pull/320), DoD PASS, QA PASS 95/100 over 4 gates + /review-pr, CI 4/4) — `setup-consumer.sh` no longer re-implements tracker resolution, it **delegates** to `resolve-platform.sh` in a subshell and its local awk parser is deleted; parity is structural rather than maintained by hand. **A and B, not A or B**: the task framed three exclusive options, but they fix different rows — the `bitbucket` and TAB cases are config-*parsing* divergences (A), while the `.env` case is a *source* divergence A cannot fix, because delegating wholesale would delete the installer's `.env` probe and collapse into the rejected Option C. So `resolve-platform.sh` learned to read `.env` (below env, below config) and delegation then *preserves* the probe. ⚠️ **Behaviour change**: no `tracker:` key + a `JIRA_URL` in `.env` now resolves `jira` at run time; opt out with an explicit `tracker: github`. No wizard-generated config can reach it — the wizard has written a `tracker:` key since T83. **21 findings over 4 gates, and the first two cycles shipped fixes that NO TEST EXECUTED.** Cycle 1's fix for the empty-`TRACKER` case was *unreachable* — `printf "%s\n%s"` loses its trailing newline to command substitution, so both halves of the split returned `"0"` and the installer resolved the literal string **`"0"`** as a tracker, matching no classification list and leaving the filter silently inert. The suite stayed green at 2441 tests throughout. **The gap was in the FIXTURE, not the code**: `makeFixtureTarball` shipped no resolver, so every install test resolved through this repo's own checkout and the `release` origin — the copy a real consumer installs with — was exercised by nothing. **`/review-pr` then found two the QA loop had not**: the PR did not contain the PASS evidence it was being judged on (gate.4, qa.3, qa.4 untracked — anyone opening it read `FAIL 70/100`), and `platform-detection.md`, the file §7 designates the source of truth and requires to change *first*, was two commits behind the resolver it governs, asserting a `^JIRA_URL=.+` pattern neither side used any more — baked into all 38 bundled copies. Four QA cycles chased the installer's error contract exhaustively and never re-read the canonical spec against the code. **The lesson**: a green suite was never evidence here — every one of the 21 findings came from running the code against a hostile input, and a fix no test executes is a claim, not a guarantee. Tests 40 → 61 in the parity file, plus 8 new cases in the runtime resolver's own suite (which §8 named as the regression net and which nothing had extended). **Residual, deliberate**: `tracker-access.test.sh` still never asserts `TRACKER` stays `github` for its own fixtures, and `_config_skills_profile` remains a hand-rolled awk YAML parser — the same mirror-the-reader pattern this task removed for `tracker:`, and the obvious next task
- [x] **T92** Add a shellcheck CI lane for the repo's shell scripts · deps: none · touches: ci-workflows!, shell-scripts!, platform-detection~, setup-consumer~ · /develop-task docs/tasks/task.92.shellcheck-ci-lane/task.92.shellcheck-ci-lane.md — ✅ **accepted + merged** ([PR #322](https://github.com/Gamaroff/agent-skills/pull/322), DoD PASS, QA PASS 96/100 over 3 cycles + /review-pr, CI 5/5) — shell was the least-gated language here: `npm run ci` runs `prettier --check` over everything and `node --test` over the suite, and **nine of those suites *are* shell scripts executed by bash**, yet nothing statically analysed shell. T83 proved the cost — a `shellcheck scripts/setup-consumer.sh` criterion rode three QA cycles, a gate and a DoD with no step able to evaluate it, and was settled by hand with a container. Gate is `--severity=warning` over the **56 tracked source** scripts, ShellCheck **pinned to v0.11.0** and printed. All 26 pre-existing warnings resolved: **9 by a real fix, 17 by a disable with a stated reason**; zero bare suppressions remain. **The task's own recommended home would not have worked, and would have looked like it had.** §6 said put the job in `validate.yml`; that workflow is path-filtered to `skills/**` and `shared/resources/**`, which excludes `scripts/setup-consumer.sh`, `scripts/release.sh` and `.agents/scripts/backfill-story-issues.sh` — all three carrying a warning, the first being *the script that motivated the task*. A lane there would never have fired for the change that caused it to be written and would have reported green while structurally unable to fail. `test.yml` is unfiltered but guarded by `ci-gate-parity.test.mjs`, which asserts set equality with the `npm run ci` composite — so a separate one-job workflow, no path filter, ~15s. **Every one of the five findings was in code this task itself introduced**; the inherited tree was clean at `error`. The sharpest was a guard added *while fixing the SC2010* that reported a failure and then **continued**, into the exact `sed`-reads-STDIN hang its own comment claimed to prevent — `return` at the top level of an executed script is illegal, and the `2>/dev/null || true` after it swallowed both the error and the status. It is the **task.90 shape, reproduced inside the task built to catch that shape**. Unreachable in practice, so the suite stayed green throughout and would have stayed green forever; found by mutation-proving the guard, not by running the tests. Three more were numbers true once and silently not any more — **11/15 where the split is 9/17** (counting *assignments quoted*, not *findings resolved*), **"five workflows" where there are six**, and a sources-only comment still claiming **"725 vs 81"** when this very change made it 515 vs 55. Two of the 26 were **not false positives at all**: the SC2211 backticks sat inside a *double-quoted* `assert_rc` message, so bash was executing `? access` and silently stripping the emphasis. **A green suite was never evidence on this task.** ⚠️ **Independent review never ran** — three Explore subagents (the QA cycle-2 refute pass and both Step 5c lenses) hung, returned nothing, ignored wrap-up requests and were killed, one after ~40 minutes; finalise's four DoD agents were skipped on that evidence and verified in-line. Self-review found four of five findings, but the mechanism meant to catch what self-review misses was never exercised, and the DoD records that as a partial rather than ticking the box. **A human read of the 28-file reviewable surface is the outstanding mitigation** — the other 137 files are generated and isolated in `d383fa90`. That subagent failure is itself worth a task
- [x] **B6** Ten more fail-open routes past the snippet classifier, plus two over-refusals · deps: none · touches: qa-skills!, test-harness!, bundles~ · /develop-bug docs/bugs/bug.6.snippet-classifier-ten-more-fail-open-routes/bug.6.snippet-classifier-ten-more-fail-open-routes.md — ✅ **accepted + merged** ([PR #323](https://github.com/Gamaroff/agent-skills/pull/323), DoD 10/10 PASS, CI 5/5) — two verify cycles; the fix's own first cut opened two NEW fail-open routes that the 13-input corpus passed clean

---

## Deferred / human-gated

Rows here are invisible to selection. Move a row up into a phase when it becomes actionable.

- [ ] **T41-fixtures** Capture real GitHub Projects v2 payloads from a second board (option ids are per-project; T39's fixture set needs two boards with differing `Done` ids) · manual — requires creating a scratch Projects v2 board
- [ ] **T38-fixtures** Capture `rapp-story-ready-for-showcase.json` from the live RAPP board · manual — requires Jira credentials this repo does not hold

---

## Housekeeping

- [ ] Archive accepted rows to `roadmap-history.md` at each phase close
- [ ] Tick Phase 5 rows `[x]` as each task is accepted — an unticked row for an accepted task is still selected and stalls the loop

---

## Change Log

| Date       | Change                                                          |
| ---------- | --------------------------------------------------------------- |
| 2026-08-03 | Roadmap created; Phase 1 seeded with the tracker-workflow series (T36–T41) |
| 2026-08-04 | T36 accepted — PR #190 merged (`48bb57e`). One success criterion waived: migration recipe unverified against a live board |
| 2026-08-04 | T37 accepted — PR #193 merged (`deeb795`). All 17 success criteria met, no waivers. 5 QA cycles, 9 findings closed; tests 760 → 840. **T38 and T39 are now unblocked** — they may run in parallel |
| 2026-08-12 | Phase 2 seeded with the document change-log series (T42–T45, issues #201–204). Strictly sequential via `deps:`; T45 `manual`-gated on verifying T44's advisory default. Legend gained `change-log`, `templates`, `review-skills` tags |
| 2026-08-12 | T39 accepted — PR #206 merged (`2c7ff3e`). `gh-stage.js` (1,299 lines), 65 tests, 10 fixtures. All 16 success criteria met, no waivers. 5 QA cycles, 20 findings closed; tests 1050 → 1065. The same defect — writing a status to a board the operator did not name — was caught twice, the second time reopened by a fix's own partial-read tolerance. **T40 is now unblocked** |
| 2026-08-12 | T44 accepted — PR #211 merged (`bd7dcdc`). Fourteen skills now write a Change Log row on mutation; the four `review-*` grade presence + currency per `change-log.enforcement`; `documentation-standards-validator` check (3) defined at last. 21 new protocol tests; tests 1158 → 1175. 1 QA cycle, 1 finding closed. **T45's operator gate is satisfied**: the `advisory` default was verified live rather than by the synthetic check the gate proposes — T44's own document predated T43's template, so its Step 2 review exercised the legacy path and returned **GO with one Important finding** at 9/10. The one defect QA found (an out-of-order instruction list that made an unconditional write read as conditional) passed lint, all 1175 tests, the bundler, both eval suites and doc-link resolution — in a skills library the instruction files are the product, so that class is a real gap in automated coverage |
| 2026-08-12 | T40 accepted — PR #207 merged (`f3d37be`). Five inline board blocks → `gh-stage.js` calls; ~240 lines of duplicated GraphQL deleted. **The GitHub path is now live** — a consumer's ladder drives their board. Three intended behavioural changes (regress guard, case-insensitive Done, honest post-condition). 2 QA cycles, 1 MEDIUM + 3 LOW closed; tests 1065 → 1070, all 5 new guards mutation-tested. Found on the way: **the bundler cannot see `.agents/skills/…/references/X` paths**, so writing a CLI call did not ship it — each site now names `shared/resources/gh-stage.js` and CI gained a bundle-freshness check. Two consumer tests deferred (no scratch board with bespoke columns). **T41 is now unblocked** (deps T38, T40 — both shipped) |
| 2026-08-12 | T41 accepted — PR #208 merged (`0b9d336`). **The tracker-workflow series is complete**: `changes-requested` (per QA fix cycle) and `pr-merged` (from the merging orchestrators, inside `develop-batch`'s per-item loop) now fire; `setup-consumer.sh` scaffolds `tracker-workflow.yaml` and never overwrites; `--init-workflow [--force]` and `--check [--offline]` on both stage CLIs; `develop-bug` reaches parity after signalling nothing for a release. All 15 success criteria met, no waivers. 2 QA cycles, 3 bugs closed; tests 1070 → 1104. Both new moments are off by default and were verified inert against a live board. Found on the way: **the scaffolder inferred "file written" from an exit code this CLI family deliberately returns as 0 on write-nothing skips** — an unauthenticated `gh` would have left consumers with no workflow file while the wizard reported success. Caught by *executing* the code against throwaway consumer repos, not by reading it, and the fix verified by reintroducing the defect to prove the regression test fails. Two residuals named: GitHub's live-probe branch is unreachable from the wizard by construction, and Jira's `--check` board half compares against a local record rather than a live probe. **Phase 2 (T42–T45) is now the frontier** |
| 2026-08-12 | T42 accepted — PR #209 merged (`5b94058`). **Phase 2 has its foundation**: one canonical Change Log spec (`document-change-log.md`) and one engine (`change-log.js`, tracker-agnostic, dependency-free) replacing four incompatible table shapes and a Jira-only implementation. Three long-standing defects die with the extraction — H2-only heading match (which inserted a *second* block above the Epic Goal), an end-scan that swallowed sibling subsections, and the top-of-body insertion fallback — plus a new guard: a marker or heading inside a fenced block **or inline code span** is an example, not a section. No skill behaviour changes; `jira-sync.js` keeps every old export as a wrapper. 14/15 success criteria met; 1 recorded as **not met with reasoning** rather than reworded, because it contradicted the task's own breaking changes. 3 QA cycles, 6 defects closed (2 HIGH, 1 MEDIUM, 3 LOW); tests 1104 → 1144. Found on the way: **every one of the six defects was the same mistake** — a rule stated correctly in the spec, then applied to a subset of the places it governs — and the inline-code half of the fence guard was found by pointing the finished engine at the document that specified it. **T43 is now unblocked** |
| 2026-08-12 | T43 accepted — PR #210 merged (`7dd5e24`). **Every template now emits the canonical section and every `create-*` skill seeds row one**, so a newly created PRD / epic / story / task opens with one Change Log row. Placement follows the sign-off precedent: unnumbered on tasks (the 11-section count re-asserted in the same test block), promoted out of `## Notes & Updates` to its own H2 on epics, and left nested as `### Change Log` on PRDs. All 12 success criteria met, no waivers. 2 QA cycles, 4 findings closed; tests 1144 → 1158. **The documented drift was half the real drift**: two epic-template copies were described as byte-equal and were not — one carried a wholly different frontmatter schema hidden behind an identical line count, so `wc -l` had been mistaken for equality. Phase 2 therefore *replaced* a schema rather than copying three lines, which is a decision; the review made it explicit and added the pre-lock consumer grep that then caught `prd-structure-guide.md` documenting the old shape. **Every defect QA found was in the new instructions, not the templates** — two told an agent to bump a frontmatter `updated:` field its document type does not have, and fixing the first surfaced a second instance the code review had missed; the invariant is now swept for. Two residuals named rather than silently shipped: `review-prd` still writes a five-cell row into the now-four-column brownfield table (**a blocking condition on T44**, as this task's own Risk 4 predicted), and the brownfield *architecture* template carries the same five-column form while its three siblings do not — outside the canonical spec's scope. **T44 is now unblocked** |
| 2026-08-13 | T45 accepted — PR #213 merged (`1aba9f1`). **Phase 2 is complete.** The pipeline now records itself and the sync stops narrating: `develop`, `qa-story`/`qa-task`, `qa-fix` and `finalise` write milestone rows, while the six sync skills narrow from a row per body-hash refresh to two events — issue created, status transition. One marker pair replaces two; the task.42 compatibility wrappers are deleted rather than left orphaned. 20/21 success criteria met; **live-Jira verification recorded as not met** (no credentials, GitHub-tracked repo) rather than quietly ticked. 2 QA cycles, 3 bugs closed; tests 1175 → 1185. The demonstration is the document itself: task.45's own Change Log is eight rows written by the code it ships, with `Version` moving only at creation, review and acceptance. Found on the way, and the reason this row is long: **the engine silently deleted every Change Log row it could not parse** — `upsertChangeLog` regenerated blocks from rows passing a date-first predicate, so a log ordered `\| Version \| Date \|` lost its entire history on first write. This repo's own roadmap template shipped with that ordering. Pre-existing in T42 and not a regression here, but T45 routes five more writers into that path, and "never drops a row it parsed" was the mitigation T45 claimed for its own Critical risk — true, and hollow, since the rows it drops are the ones it fails to parse. Fixed, with the template corrected. Two further T42 engine defects (content loss on the hand-written-heading path; the same-pair collapse skip) were **named and left** rather than folded in mid-QA-cycle. Also caught: a bulk section-replacement regex whose next-heading lookahead matched a heading *inside a fenced sample*, leaving orphaned legacy blocks and unbalanced fences in all six sync skills — the same context-blind class of error recurred once more during finalise and was caught by a guard count |
| 2026-08-13 | **Phases 1 and 2 archived** to `roadmap-history.md` at phase close, per the standing Housekeeping row — the first time that file has been cut, so the header no longer promises a document that did not exist. Ten accepted rows (T36–T45) moved verbatim, keeping their `touches:` tags and acceptance annotations so a `deps:` naming any of them still resolves. The two `-fixtures` rows stay here, deferred and human-gated. This Change Log is deliberately **not** split: it is append-only and it is this document's own history, so the archive references it rather than copying ten long entries out of it. No frontier remains — `select-next.mjs` reports `roadmap-complete` until a new phase is added |
| 2026-08-28 | Phase 3 seeded with the `loop-supervisor` series (T62–T64). Legend gained the `loop-supervisor` tag. T62 is the only mandatory unit; T63/T64 depend on it and hard-conflict with each other on `run-loop.mjs`. Design: `.agents/plans/loop-supervisor.md` |
| 2026-08-29 | **Phase 3 closed and archived.** T62–T64 moved verbatim to `roadmap-history.md`, clearing the live roadmap of completed rows per the standing housekeeping item. Their `deps:` references resolve there, as Phases 1 and 2 already do. No phase now holds an actionable row, so `select-next.mjs` reports `roadmap-complete`; the two Deferred fixture rows (T38/T41) stay invisible to selection and both need credentials this repo does not hold. Filed `bug.2.unbounded-test-concurrency` at the same time — the parallel-pool flake that T62 and T63 were each merged over, which those rows twice said needed its own bug |
| 2026-08-29 | T64 accepted — PR #278 merged. `run-loop.mjs` gains `--dashboard`/`--dashboard-token`: a status frame POSTed at each iteration boundary, ending `active: false`, built from state T62 already writes and adding none. What ships is a **documented payload, not an integration** — the dashboard lives in the consumer repo, so both halves can be built independently and a second consumer is served unchanged. Plus `docs/runbooks/unattended-overnight-runs.md` and the `develop-next` cross-references pointing at the fresh-context alternative to `/loop`. 8/8 success criteria; tests 1834 → 1870. **Phase 3 (T62–T64) is complete.** The reason this row is long: **three QA cycles, twelve findings, on work whose suite was green and whose CI passed from the first push** — nothing a re-run would ever have surfaced. Gate 1 was CONCERNS at **50/100**, and the findings share one shape, the exact failure this task was written to prevent: *coverage claimed where none existed.* The frame published the **whole append-only ledger** while its own README promised per-run totals, so a second run in the same tree would render last night's iterations as current. The token-absence test **could not fail** — proved vacuous by deleting the header it guarded and watching it stay green — while the Risk Assessment rated that leak Low/HIGH and called it "asserted by test". SC2 was proved one level *below* where the criterion states it (at `pushDashboard`, not at the run). And the Risk Assessment named `tests/executable-instructions.test.js` as its mitigation for "runbook documents commands that do not ship" — a test whose `collectDocs()` **had never opened a runbook or a skill README**, so the two documents this task created were unscanned by the gate cited to protect them. Widened and mutation-proved on both. Cycle 2 then re-verified every cycle-1 fix **by mutation rather than by reading it**, and found one more of the same family: the child-environment token strip was correct in code and **held by nothing** — it sat inline in `main()`, unreachable from any test, so deleting it left the suite fully green. That is cycle 1's vacuous test wearing different clothes, so it gated rather than being waved through; `childEnvFor()` is now exported with tests in **both** directions, because a single "the token is gone" assertion would have passed against a function returning `{}` and breaking every spawned iteration. 10 invariants probed, 10 proved. **Two further defects were introduced by the fixes themselves and caught before CI**: the double-SIGINT fix opened a third-SIGINT re-entrancy race (found by the adversarial pass over the fixes, not by QA), and the QA report linked `.summaries/`, which `.gitignore` excludes — a link that resolves locally and 404s in CI, found by checking the **tracked** tree in a detached worktree after CI had already caught one dead anchor earlier in the run. Unlike T62 and T63, this merged on a **fully green local suite and green CI on the final head**; the parallel-pool flake those two rows describe did not recur, and still wants its own bug |
| 2026-08-29 | T63 accepted — PR #277 merged (`cbc909b`). `run-loop.mjs` gains `status` and `watch` — pure readers over the artifacts T62 writes, safe from a second terminal, over SSH, concurrently and mid-iteration — plus `--notify`/`--webhook` firing **once** on terminal stop. The renderer is a pure function both views wrap (`references/render.js`), with `nowMs` and `isAlive` **injected**, which is the choice that makes the dead-pid state testable without arranging a real dead process. 9/9 success criteria met, plus a tenth QA added. 2 QA cycles, 1 MEDIUM closed; tests 1794 → 1834 (150 for the skill), **7 invariants mutation-proved**. Found on the way, and the reason this row is long: **the task document described the ledger inaccurately, and both the fix and the review of the fix repeated the same mistake in new forms.** Review caught `numTurns` (the ledger writes `turns` — `num_turns` is the *envelope's* name) and an undocumented second row shape (`spawned: false` probe-stop rows carry seven fewer fields and are the **normal last line of a healthy run**), before either reached the renderer. QA cycle 1 then found the real defect: `readCurrent` collapsed *absent* and *unparseable* into `null`, so a torn heartbeat rendered as **"no run in flight"** — the reassuring answer at the moment it was least deserved, and the same failure the task's own Decisions table exists to prevent, worse in one respect because a crashed report makes someone look while "no run in flight" ends the investigation. Fixed at both ends (reader distinguishes the two from one `readFileSync` and the error code; writer is now atomic via temp+rename) — and the reader fix was **kept despite** the writer fix, because a view should not depend on the writer being careful in order to avoid stating a falsehood. QA cycle 2 then found that the *fix* had introduced the same shape pointed the other way: the new sentinel was duck-typed across a module boundary, so a valid heartbeat carrying that key was misreported as unreadable. Now compared by identity. **Merged over a red local `npm test` on operator decision, as T62 was** — but this time the flake was pinned down rather than tolerated: a controlled experiment (clean `develop` plus two *filler* test files containing 30 trivial assertions and no task code) fails **five** tests including the same `§8b`, while this branch fails one and CI passes both. The suite has a pre-existing parallel-pool fragility — five spawn-heavy tests carrying 20–30s wall-clock budgets for work that completes in under a second — and it needs its own bug; the likely fix is bounding `node --test` concurrency, not raising five timeouts. **T64 is now unblocked** (it hard-conflicts with T63 on `run-loop.mjs`, so the conflict is now resolved rather than pending) |
| 2026-08-28 | T62 accepted — PR #276 merged (`c113362`). `skills/loop-supervisor` ships the supervisor process and its artifacts: `run-loop.mjs`, the pure `classify.js`, three adapters, pinned `--settings`, and 110 unit tests. 7 of 8 success criteria met; **SC5 (one real `/develop-next` iteration) deferred to a post-merge operator step** — it cannot run inside the pipeline implementing it, which holds both `develop-next.state.json` and `develop-pipeline.lock` and would merge an unrelated roadmap item as a side effect of testing. 2 QA cycles, 1 finding closed: config silently overrode an explicit CLI flag equal to the default, which would have pointed the progress oracle at the wrong ref and made every healthy iteration read as `idle`. The DoD then caught a gap both QA cycles missed — no `CHANGELOG.md` entry. Merged over a red local `npm test` on operator decision: the one failure is a pre-existing 30s timeout in `jira-interception.test.mjs` that reproduces on clean `develop`, passes 48/48 in isolation, and passes in CI on this head. **T63 and T64 are now unblocked** — they hard-conflict on `run-loop.mjs`, so `--batch` will take only one at a time |
| 2026-08-29 | B2 closed — PR #279 merged (`6bfc65a`). `npm test` now bounds `node --test` on all five invocations, and a new shared `spawn-budget.mjs` (60 s, env-tunable, `{PREFIX}_` → `TEST_` → default) replaces **all eleven** bare timeout literals in `jira-interception.test.mjs` and `access-config-parity.test.mjs`. **Measurement inverted the fix the bug asked for.** Bounding is free — 36–39 s across unbounded/4/8/unbounded — but on a quiet box it buys nothing: the worst single test is ~2.6–2.8 s at *every* concurrency including unbounded, and the spread between two **unbounded** runs is as wide as bounded-vs-unbounded, i.e. the suite never self-oversubscribes enough to threaten a 20 s timeout. What collapses the margin is external load the suite does not own — 16 competing spawn loops put the worst test at 16.2 s unbounded and **17.1 s bounded** — so option 1 cannot help there. The detail that settled it: the file that *absorbed* that 16.2 s spike was `access-config-parity.test.mjs`, the one file already carrying a 60 s budget, while `jira-interception.test.mjs` (the historical victim) peaked at 4.3 s. **Shipping the bound alone would have closed this bug without changing the failure probability** — the worst outcome available, since the flake returns against a bug marked fixed. Both halves therefore shipped, and the parity suite's remedy was extracted rather than copied: it had applied its own fix to 1 of its 12 spawn sites. The reason this row is long: **four of five verify cycles failed, and every failure was in the guard rather than the fix** — fitting for a bug about a defective gate, and three of them the same shape, *a guard passing on the exact regression it was named after*. Cycle 1: prettier, which CI runs **before** the suite. Cycle 2: a comment stripper that deleted 53 lines of a real suite (a `/*` inside a `//` opened a phantom block), a detector blind to `node --flag --test`, `SPAWN_RETRIES=0` silently becoming 2, and `??` letting an **empty** `{PREFIX}_` var mask the global knob — the normal shape of an unset CI input, so it broke in precisely the tune-the-CI-box case the module exists for. Cycle 3: the *fixed* stripper deleted a line after every regex literal ending in `\/` (11 real sites) and preserved comments after `/['"]/`; two guards were also proved to be measuring nothing — `runnerScripts()` was never called by its own tests, so reverting it changed nothing observable. Cycle 4: the scanner still did not lex template substitutions, so a nested template containing a URL or glob deleted to EOF — a construct already present in the corpus. The lesson recorded in the bug: **a regex cannot lex JavaScript**, and *the fixture, not the implementation, decides whether a guard is worth anything* — each broken version had a passing test whose fixture omitted the one construct that broke it. Settled by a whole-corpus check (strip all 74 scanned files, parse the output, expect zero failures — an earlier revision produced five) and 10 mutations each reddening exactly one guard. Also found: a mutation using `sed '0,/re/s//repl/'` silently did nothing on BSD sed, and the resulting green suite read as an unheld test — **verify the mutation, not just the test**. Tests 1877 → 1893. Four residual limitations recorded rather than buried, incl. `TEST_CONCURRENCY=0` being silently ignored by node, which no static guard can see. Adjacent and explicitly out of scope: `package.json`'s test globs are hand-maintained, 11 of 21 one-skill-one-line, so a new `skills/*/tests/` dir is silently uncovered — nothing orphaned today |
| 2026-08-29 | **Phase 4 retired**, not merely emptied — its two rows (B2 accepted, T65 in flight) moved to `roadmap-history.md`, and the "How to use" preamble rewritten to say why no standing maintenance phase should replace it. T65 makes the phase's own stated purpose obsolete: selection now falls through to the bug and task registries when no phase holds an actionable row, so filed work reaches the loop without the hand transcription this phase *was*. The archival is part of T65's delivery rather than a follow-up, because roadmap precedence is absolute — while Phase 4 held an actionable row the new fallback could never fire, and the feature would have merged and changed nothing observable. T65's row is archived **unticked** (it was in flight); its acceptance is recorded here on merge |
| 2026-08-29 | T65 accepted — PR #281 merged (`f0d06c2`). `select-next.mjs` now falls through to `bug-registry.md` and `task-registry.md` when no phase holds an actionable row, so filed work cannot be invisible to the loop. This retires the transcription step Phase 4 *was*; the phase is archived and its T65 row is now ticked. Roadmap precedence stays absolute and only `roadmap-complete` is pre-empted — the four deliberate stops are pinned by one test each, asserting the registry loader was never *called*. **Three QA cycles, and the cycle-1 fix introduced the cycle-2 defect**: the floor admitted `ready-for-review`, a status `develop-task` HALTs on, so the frontier could nominate work the dispatcher refuses and an unattended loop could not self-recover; fixing that exposed the row-visibility guard, whose own fix then made *non*-rows falsely visible in the same `--lint` report. Both were mutation-proved, and cycle 3 verified a claim about a **test** — that one had been rewritten because it could not fail — by reverting the fixture (0 red before, 1 red after). The DoD gate then caught two things CI could not: `CHANGELOG.md` still described the pre-fix eligibility floor, and `task-registry.md` row 65 was stale. Gate PASS 90/100, 12/12 criteria, tests 1924 → 1946. Carried forward: `--lint` reads one document per registry row (linear, unbounded, operator-invoked only), plus two residual limitations on already-malformed markdown |
| 2026-09-01 | T67 accepted — PR #289 merged (`261fe76`). QA PASS 90/100, 2 cycles. **36 attack inputs → 0 reach `runnable`**; 2075 tests, 0 failures; 21 mutation proofs, all held. Every gate caught what the previous one missed: the review found a missing `npm run bundle` (CI would have gone red), QA cycle 1 found 13 fail-open holes with a green suite, and the **DoD gate found 14 more** — two of them commands deny-listed by name, reached through quoted spellings. Follow-up filed in the task Notes: make `zsh` a CI prerequisite (9 tests, incl. the task-66 fixture, skip silently on `ubuntu-latest`) |
| 2026-09-01 | **T73–T75 filed** from the task.67 pipeline retrospective, appended to Phase 5. All three fix gates that read rather than probe: the DoD security prompt greps where it should execute (T73 — a substituted probing prompt found 14 fail-open routes the grep version passed); a security re-review scopes to the diff, so cycle 2 re-asked the question cycle 1 had answered (T74); and the quality gate runs `npm test` while CI runs `format:check` + `npm test` + `eval:all`, so two of three never ran locally (T75). New `finalise` tag added to the legend — there was no tag covering `skills/finalise`, so `--batch` could not have seen a conflict there. **T75 is a candidate to pull forward**: it is the smallest of the three and every later row is verified by the gate it fixes |
| 2026-09-02 | **T74 accepted + merged** ([PR #299](https://github.com/Gamaroff/agent-skills/pull/299)). A re-review after a safety failure now re-probes the surface instead of re-reading the fixes — the rule stated once in `shared/resources/qa-re-review-scope.md`, wired into both QA skills as a disjunct on the existing `PRIOR_GATES` guard, and held by a 34-test parity suite whose replay cases **execute** the trigger against real gate fixtures. Scope corrected before implementing: `61197c3` had already given cycle 2 a full-diff refute pass the same day the task was filed, so the task was re-aimed at the genuine residual gap (cycle 3+). The run repeatedly caught itself: a `\s` GNU escape that would have failed the trigger closed and silently; a probe that *hung* on an empty `$LATEST_GATE`; a cycle-1 fix recorded as done that changed nothing observable (found by the cycle-2 refute pass); two vacuous regression tests that passed; one `SKILL.md` overwritten by its sibling with all 28 tests still green; and at the DoD gate a dead link that turned CI red plus a **third** stale copy of the scoping rule in `code-review-prompt.md` |
| 2026-09-01 | **T76 filed** — the fourth lesson from the task.67 retrospective. `mutation-proving.md` answers "is this test real?" well and never says what a suite of real tests still fails to cover: 9 proofs held on task.67 while 13 fail-open routes sat in the code. It also gives an unheld proof one diagnosis (vacuous test) where there are three — both of task.67's unheld proofs were true signals (dead code; a finding whose mechanism was factually wrong) that the current advice would have discarded. Doc-only, no SKILL.md touched — consumers inherit it by reference. New `mutation-proving` tag added to the legend |
| 2026-09-01 | **T77 filed** — `/review-pr` joins the develop pipelines. Its **code** lens duplicates what `qa-story`/`qa-task` already dispatch every cycle, which is why task 66 shipped it standalone; its **conformance** lens (coverage, scope, trail, consistency) has no counterpart anywhere in the pipeline, so a run can reach `accepted` on a trail that does not hold. Not hypothetical: the one time the instrument was pointed at a pipeline-built PR (#283, its own) it returned REQUEST CHANGES and produced T67–T70. Wired as **Step 5c**, the exit gate of the Step 5–6 QA loop — not a ninth step, because `current_step` is an integer validated `1..8` and a renumber would ripple through the lock, both hook `case` blocks, the resume detector, both progress templates and every `{N}/8` string for zero behavioural gain. `REQUEST CHANGES` routes back to `qa-fix` on the shared 5-cycle budget; `ready-for-merge` moves behind the review. **`develop-next` and `develop-batch` need no edit** — they delegate at Step 2, so every PR they merge is pre-reviewed by inheritance; `develop-bug` is untouched (own verify-loop file). New `docs-pipeline` tag added to the legend — `docs-config` covers only two reference files, so a parallel run editing the runbooks would have shown `--batch` no conflict. Carries an acceptance-gating documentation phase: 3 mermaid diagrams, 7 runbooks and 8 reference docs restate the pipeline shape independently, and `pipeline-artifacts.md:50` currently calls the skill "not a pipeline step" |
| 2026-09-01 | **B3 filed and placed at the head of Phase 5**, and **T75 pulled forward to second** — executing the intent this log recorded when T75 was filed. B3 is a gate defect, not a feature: three CLIs call `process.exit()` immediately after an async `stdout.write`, so piped output truncates at the ~64KB buffer while a file redirect stays complete. `select-next.mjs --lint` crossed that line as Phase 5 filled up (68,006 bytes to a file, 65,266 to a pipe), so `npm test` is **red on a clean tree** — and `developNext.qualityGateCommand` defaults to `npm test`, which SKILL.md:156 calls "the real gate" run **always** before `gh pr merge`. Nothing in the frontier can merge until B3 lands, which is why it outranks every task. Candidate fix verified (`process.exitCode` + drain: 123 pass / 0 fail) and **mutation-proven** — restoring `process.exit` returns the suite to 122/1. Two further latent instances found and recorded rather than fixed blind; the most consequential is `qa-execute-snippets.mjs:1006`, the task-67 machinery whose `--json` report scales with snippet count, so the gate built to stop unverified work would itself fail to parse on the largest skills. T75 sits second because it widens that same gate to `format:check` + `npm test` + `eval:all`, and every row after it is then verified by the gate it fixes — worth nothing while the suite is red, worth a lot the moment it is green |
| 2026-09-01 | **B3 accepted — PR #290 merged** (`459428e`). The merge gate is green again: `npm test` was red on a clean tree because `select-next.mjs --lint` crossed the ~64KB pipe buffer, and `npm test` is `developNext.qualityGateCommand`, so **every** PR was blocked behind it. Root cause is one Node idiom — `process.exit()` does not flush an async write, and stdio is async on a pipe but sync on a file, which is why the defect was invisible to every by-hand run and visible only to the machine callers that matter. All **10** exit-after-write sites fixed (the bug named 4), across 3 CLIs + 7 bundled copies; `generate-prd-epic-index.mjs` gained a `main()` wrapper because top-level `return` is illegal in ESM. New 10-test guard suite in four layers, mutation-proven 3×; DoD 9/9; 1 fix iteration, 0 QA fix cycles. **Two findings worth carrying forward**: (1) the first version of the structural guard *passed under mutation* — it scanned back six lines and the write it needed to see is a ~20-line `JSON.stringify`, so it would not have caught the bug it was written for; a guard is decoration until it has been proven by reverting the behaviour it claims to hold. (2) CI caught a `format:check` failure that a green local `npm test` could not, because `npm test` does not run `format:check` — which is precisely what **T75** (next in this phase) exists to close. The same idiom survives in **15 further files**, three of which pipe orchestrator JSON and are latent 64KB bugs of the identical shape; they are named in `KNOWN_UNMIGRATED` in the guard, which fails if an entry goes stale, and warrant a follow-up bug |
| 2026-09-01 | **T75 accepted — PR #291 merged** (`a930416`). CI and the pipeline gate now share one definition of green: `npm run ci` (`format:check && npm test && eval:all`) and `npm run ci:fast` (formatting + tests). `developNext.qualityGateCommand` defaults to the full composite; a new `develop.fastGateCommand` (default `ci:fast`) is what the develop loop and each qa-fix cycle run. **Tiering is the substance, not a detail**: formatting moves *into* the loop because its absence is exactly what shipped the task-67 red build and it costs seconds, while `eval:all` stays *out* because paying it per iteration is what makes the correct fix feel expensive enough to be reverted. The doc sweep turned out to be **6 sites, not the 2 the task named** — `develop-batch` reads the same key for its own merge gate, so a stale table there would have had two sibling orchestrators documenting different defaults for one key; that widening came out of the review, not the plan. New `ci-gate-parity.test.mjs` asserts workflow↔composite set equality in **both** directions (a CI step the composite does not call is the original defect re-created one step at a time) and is **mutation-proved 10×**. Phase 3 correctly required **no edit** — the workflow already called the three scripts — and was verified rather than changed, which the review predicted. **The gate governed its own delivery three times**: `npm run ci` went red on its first run on `prettier --check` alone (the exact task-67 shape, now caught before push); the cycle-2 fast gate went red and no commit was made on it; and the 2-attempt retry budget written *during* cycle 2 is what decided that retry. 3 QA cycles (CONCERNS 90 → CONCERNS 80 → PASS 100), 5 findings raised and closed, DoD 10/10. **Three findings recorded but deliberately not folded in** — each wants its own item: (1) `qa-execute-snippets.mjs` exits 0 with **zero output** when invoked through `.agents/skills/…`, the path its own docs prescribe, because that is a symlink and its entrypoint guard compares a symlinked `argv[1]` against a realpath'd `import.meta.url` — the QA step built to catch prose that never executes is itself never executing, and `select-next.mjs:1486` already carries the exact fix *with a comment describing this defect*; (2) `access-config-parity` failed **2 of 3** full runs under load (`spawnSync ETIMEDOUT`, 32/32 in isolation) and T75 is precisely what promotes it onto the mandatory merge path; (3) `develop-bug`'s per-cycle fix loop has no fast gate though its develop loop does |
| 2026-09-01 | **B4, B5 and T78 filed from the task.75 QA trail, and B4/B5 placed at the head of the frontier ahead of T68.** All three were found during T75's three QA cycles and deliberately kept out of that PR rather than quietly widening it. **B4** — `qa-execute-snippets.mjs` guards its entrypoint with a raw `import.meta.url === pathToFileURL(process.argv[1])`; `.agents/skills` is a symlink, so through the path its own documentation prescribes the engine exits **0 with zero output**. The QA step built to catch prose that is never executed is itself never executed, and reports success — so the Step 4b line in every `qa-task`/`qa-story` report since task 67 is unsupported. It outranks everything because it is evidence integrity, not a missing feature, and the fix is ~6 lines: `select-next.mjs:1492` already carries `isInvokedDirectly()` **with a comment describing this exact defect**, and a scan shows 3 of the repo's 4 ESM CLIs already use the resolved form — this is an outlier, not a pattern. Needs **two** guards (behavioural: invoke through a symlink and assert non-empty output; structural: scan every ESM CLI), because this repo has already shipped a structural guard that passed under mutation on the bug it named. **B5** — the `access-config-parity` failures during T75 were *not* an untuned timeout, which was the first diagnosis and was wrong. The file's own docstring at `:131` documents this precise defect and fixes it for `shellAnswer()`: a child that never ran is not an answer, so retry then throw. `jsAnswer()` at `:184` never got the same treatment — it catches only a *throw*, and the production reader correctly fail-closes to `manual` on a `spawnSync` timeout without throwing, so a timeout is recorded as a real reading and the suite reports a reader divergence that did not happen. Fix is to mirror the shell-side treatment on the JS side; **do not** raise the already-tuned budget and **do not** touch the reader's fail-closed default, which is safety-critical. It is second because T75 promoted that suite onto the mandatory merge path, and a gate failing two-in-three under load teaches people to re-run until green — the erosion T75 exists to prevent. **T78** — T75 gave the develop loop and each qa-fix cycle a fast gate; `develop-bug` shares the first document and so picked that half up for free, but its per-cycle verify loop is a separate file that got nothing, so a bug fix cycle can still commit an unformatted tree. Placed last: a real gap, bounded, nobody currently harmed. It carries one explicit warning — do not port the `MAX_ITER` retry wording T75 removed as a claim the machinery does not support |
| 2026-09-01 | **B4 accepted — PR #292 merged** (`541b604`). The snippet engine now runs through the path its own documentation prescribes. `qa-execute-snippets.mjs` guarded `main()` by comparing a realpath-resolved `import.meta.url` against a raw `process.argv[1]`; `.agents/skills` is a symlink, so the guard was false and the process exited **0 with zero output** — indistinguishable from a clean run. Fixed with the `isInvokedDirectly()` helper `select-next.mjs:1492` already carried, bundled to the four generated copies that the documented invocations actually name. Two things are worth carrying forward. **First, the scan is the transmission mechanism the repo lacked** — three of four ESM CLIs already had this fix, one with a comment naming the exact failure, and none of that reached the fourth file; the structural guard now fails on the naive shape in any engine copy, so the next file cannot repeat it quietly. **Second, a structural scan fails silently toward "pass", and did so twice in this run**: the anti-vacuous assertion was satisfied by the `node:fs` import list alone (mutation 3 passed with the guard deleted), and the regex anchored on operand order so the commuted defect slipped through — both caught only by *executing* mutations, not by reading the assertions. That is the same trap B3 shipped, now hit twice more in one task, and the standing lesson is that a scan is evidence about shape and never about behaviour. Three verify cycles were needed, and **both rejections were in the tests, never the fix** — the six-line production change reviewed clean on the first pass and was never modified after Iteration 1; what took the cycles was an unbudgeted spawn and then an unguarded dereference that would have reported a divergence that never happened under load. On a bug about a false pass, tests that can produce a false failure are not a lesser problem. Frontier advances to **B5**, which the merge-gate runs in this task exercised repeatedly: `access-config-parity` failed on two of three loaded `ci:fast` runs and passed alone on both a clean `develop` worktree and the branch, which is exactly the timeout-recorded-as-an-answer that B5 describes |
| 2026-09-01 | B5 accepted — PR #293 merged (`930caaa`). `probeResolver` returned the same `{mode: null, reason}` shape whether the resolver **ran and refused** or the child **never ran**, so the parity suite compared a timed-out probe against a correct one and reported `mode-approve [tier=awk]: shell=approve js=manual` — a reader divergence that had not happened, on a suite T75 had just put on the mandatory merge gate. Every `probeResolver` return now carries a `kind` (`never-ran` / `refused` / `null`, total across all five paths), surfaced through an observation-only `onDiagnostic` that fires **outside** `warnOnce`'s per-process dedupe — a deduplicated diagnostic would make a retry loop go blind on its second failure. Production semantics are untouched: all six call sites pass no sink and take a byte-identical path. Six regression tests; **five independent mutation reverts, all red**. DoD 6/6, CI 4/4 green on the final head. **The filed bug was half the work.** `_configAccessMemo` was caching the timeout under the file path, making the fail-closed `manual` sticky for the process *and* silently defeating the retry the report prescribed — the prescribed remedy could not have worked without fixing it first. Then **two of the three high findings in verify cycle 1 were defects the fix itself introduced**: the new probe-timeout knob read ambient `process.env`, so it resolved *after* `loadDotEnv()` and re-opened the door every gate's pre-`loadDotEnv` access snapshot exists to shut — the justification for allowing it ("no timeout value can escalate access") was answering the wrong invariant, since `gh-stage.js:726-731` is explicit that a dot-env file must not be able to **restrict**, or via a typo hard-fail, every pipeline step behind the resolver's back; and `/^\d+$/` accepted a 400-digit string that `Number()` makes `Infinity`, which `spawnSync` rejects with `ERR_OUT_OF_RANGE` — thrown from inside the one function whose docstring carries the cycle-4 incident and the words NEVER THROWS. A merely-huge finite budget was quieter and no better: it does not lengthen the timeout, it removes it. Both closed under mutation proof. Also caught: one mutation revert **reported a false green** because prettier had reflowed the target line and the mutation string never matched — recorded because a mutation test that cannot prove it mutated is not evidence, which is the same shape as the guard that once passed on the exact regression it named. The standing lesson: fixing one side of a symmetry does not fix the symmetry — `shellAnswer` carried this exact fix, with a docstring describing this exact failure, for a whole release while `jsAnswer` sat three functions away untouched |
| 2026-09-01 | **T68 accepted — PR #294 merged** (`5d2aca4`). `/review-code` Step 4 chose its PR-comment path by testing `TRACKER=github`, but a comment on a **pull request** is a property of where the code is hosted, not of where the issues live. With `VCS=bitbucket` and a GitHub or Jira tracker it took the `gh` arm against a Bitbucket PR, which `gh` cannot address — the comment never landed and the run reported success. The arm it fell through to was labelled *"Bitbucket / Jira"* (a VCS grouped with a tracker as though they were alternatives to each other — the same category error that produced the wrong key) and delegated to *"mirror `/qa-story` step 6"*, **a step that does not exist**: `/qa-story`'s main flow lives under an unnumbered heading, so an implementer found nothing to mirror. The review sharpened that claim before development began — the filed task said the step was "GitHub-only", which understates it. Fixed on the `$VCS` axis, with the rule stated in `review-pr`'s exact words and a contract test asserting the two skills state it **identically**, so rewording one fails until the other follows. `skills/review-code/tests/` is new (the skill had none) with 12 tests and its `package.json` glob — confirmed in the gate log to have *actually run*, which in this repo is a distinct failure from being registered. **Two lessons, and both are the same shape as the bug.** First, QA found the new suite crashed with `ENOENT` outside this repo (`tests/` ships in the packaged skill, and two tests read siblings) — and the obvious repair would have made the guard skip *everywhere*, leaving a green suite behind; caught only because the fix was verified three ways (in-repo 0 skipped, standalone 2 skipped, and a mutation proving the guard still bites). Second, the first backgrounded fast gate reported exit 0 that belonged to a trailing `tail` rather than to npm — prettier had in fact failed, and unheeded this would have shipped precisely the red build `develop.fastGateCommand` exists to prevent. A mechanism that reports success without having checked anything is the defect class this whole item is about, and it recurred twice inside the fix for it. Sweep: 64 `TRACKER=github` occurrences across 20 source files classified; `review-code` was the **only** PR-shaped one, and recording the 63 correct hits as deliberately kept is what stops the next sweep introducing the mirror-image bug. Seven mutation reverts total (5 dev, 1 independent QA, 1 of the fix), each proved to have applied before its red was counted. Frontier advances to **T69**, which this task's Bitbucket arm explicitly defers to |
| 2026-09-01 | **T69 accepted — PR #295 merged** (`7a68e9c`). `/qa-story` step 6 and `/qa-task` Step 13 post the QA gate decision to the PR and both mark that step **BLOCKING** — with no Bitbucket arm, so on a Bitbucket repo a step the skills call mandatory could not succeed at all. Both now branch on `$VCS`, with the REST arm lifted verbatim from `qa-fix` and the GitHub arm moved to `--body-file` (a correctness fix: the body carries backticks and `$(…)`). The platform preamble was **absent from both**, not one — the review corrected Phase 3 from a spot-check to an authoring step before development began, and caught that the two skills already sourced the resolver differently, `qa-story` via a `$(dirname "$0")` form that never resolved from the repo root. Left alone, Phase 2 would have shipped a Bitbucket arm above a dead resolver line. Two new suites, 25 tests, both globs registered; T68's cross-skill drift guard reused, including the companion test that stops it skipping silently. **The defect this item is really about is the one QA found in the fix.** Moving the body into a single-quoted heredoc was right for `qa-task`, whose body uses `{SLOT}` placeholders, and silently wrong for `qa-story`, whose body used live shell variables — so it emitted a literal `$PR_NUMBER` where the PR number belonged. The comment still **posted**, so the step's own BLOCKING exit-code check passed: a step whose success criterion is an exit code cannot see a fault in its own output. It broke the one criterion the task promised to hold ("on `VCS=github`, behaviour is unchanged apart from `--body-file`"), and it reached the PR past **three recorded mutation proofs** — all three structural, so they proved the structural assertions and nothing else. Coverage looked complete because every mutation anyone thought to run was of the kind already covered. QA closed it by running a mutation development had not, then probing the *new* guard for vacuity and confirming it fails loudly when its anchor moves. **Two smaller findings, both the same shape.** The §11 dot-source coverage floor was 2 against a count of exactly 2, so the last legitimate removal was indistinguishable from the scanner going blind — its own comment claimed the floors sat below the counts; lowered to 1 with an instruction to delete the arm rather than reach 0. And `/finalise`'s doc sweep caught `review-code` still saying "no such step exists" about the step this task had just created — found only by sweeping documents that *restate* changed behaviour, which is the drift class that otherwise ships silently. Eight mutations across the task, each confirmed red before being counted; CI waited for rather than assumed (first rollup sample PENDING, re-sampled to SUCCESS on the final head). Frontier advances to **T72** |
| 2026-09-01 | **T72 accepted — PR #296 merged** (`63fa54b`). The bug half of `16/H1` asserted `BUG_ELIGIBLE_STATUSES ⊆ what develop-bug proceeds on`. That subset relation holds for **every** possible widening of the dispatcher, so it was structurally silent about precisely the drift it existed to catch — adding a fifth proceed-row left it green. It now asserts the gap **exactly** (`{in-progress, ready-for-qa}`), failing in both directions. `BUG_ELIGIBLE_STATUSES` is byte-identical, so no bug's selectability changed; what changed is what the build can detect. **The task's real content is why the gap stays.** Task 71's equality rule is not a universal law the bug axis failed to obey: `develop-task`'s pre-work statuses mean *"unstarted work is waiting"*, which is right to nominate, while `develop-bug`'s two extra statuses are **resume affordances** — `in-progress` is *"a prior run may have started"*, `ready-for-qa` is *"if a fix already exists"* — written so a re-invoked pipeline does not HALT on its own half-finished work. Selecting on them would hand an unattended loop a bug a human is holding, or one whose fix is written and only awaiting verification. **The defect this item is really about is a proof that proved the wrong thing — twice.** The task's own mutation list credited the anti-vacuity guard to a mutation of `BUG_ELIGIBLE_STATUSES`; the guard never reads that constant, so review split it before any code was written. QA cycle 2's refute pass then found the *replacement* mutation was also non-discriminating: renaming a dispatcher row yields a **three**-element gap that `deepStrictEqual` rejects anyway, so the guard merely fired first. Only **deleting** a row leaves the gap at exactly two elements and slips past the assertion — and a control run with the guard removed goes **green**, which is the actual proof it is load-bearing. Seven probes total, including one that correctly stays green (widening both sides equally) to establish the assertion pins the *difference* rather than either set. The guard's claim was true throughout; what was missing, twice, was a proof that could discriminate — the vacuous-coverage failure caught one level up. **CI red-lighted the merge gate once, on a flake, and it was diagnosed rather than retried blindly**: `stdout-drain-on-exit`'s premise test asserts a 200KB piped write gets truncated before `process.exit()`, which is a scheduling race; it generates its own temp script and has no path to this diff. Re-running the identical commit went green. Frontier advances to **T73** |
| 2026-09-02 | **T73 accepted — PR #297 merged** (`d68a0cb`). The `/finalise` DoD security agent was a grep-only inspector: every check asked whether a boundary *existed*, none asked whether it *held*. It gains a gated **probe mode** — locate the entry point, generate candidates across five axes, **execute** them, report only what reproduced, and probe the accept direction too so an over-strict fix is caught as readily as a permeable one. "Read-only" is redefined as *does not mutate*, not *does not run*: the agent already had `Bash`, and that foreclosure was the defect. **It found real defects on its first run** — replayed against the commit that closed task 67's original fourteen, it surfaced **twelve more open routes**, all reproduced deterministically on HEAD and filed as `bug.6`; three prior gates had passed that file. The task's own "replay reports none" criterion was **falsified by execution** and replaced with the assertion that actually establishes the property, argued in the document rather than quietly amended. **Four QA cycles, and the shape of them is the lesson**: the same conflation had to be chased up three levels — `probes: []` meant three things; splitting it moved the ambiguity to `boundary`, where *absent* was rendered as the confident claim "not a boundary"; fixing that made the two verdict lines double-render, so a boundary that ran nothing reported both "executed no candidates" and "the boundary held". Each state now has its own branch. **Twice a test that claimed to protect an invariant could not observe its violation** — presence assertions passing through a contradictory render, and an emptied replay corpus passing every filter; both are now structural, and one mutation proof reported green having silently failed to apply. 21 findings closed, 23 mutation proofs, HIGH count 4 → 2 → 1 → 0; tests 2157 → 2169. CI now checks out full history so the replay's discriminating half runs rather than skipping at depth 1. **Follow-up: `bug.6`** — four root causes, not twelve |
| 2026-09-02 | **T76 accepted — PR #304 merged** (`0a4ea79`). `shared/resources/mutation-proving.md` answered one question thoroughly — *is this test real?* — and was silent on the adjacent one: **what does a suite of proven-real tests still fail to cover?** It also gave an unheld proof a single diagnosis when there are three, two of which are discoveries rather than defects. Three sections close both gaps in 54 lines (140 → 194, against a ~55-line budget): the coverage limit stated once and plainly, a three-cause diagnosis table (**vacuous test · redundant source · wrong premise**) replacing the old single conclusion rather than accreting beside it, and a fifth *When to do it* row requiring a boundary fix to be proved in **both** directions. The number it carries is task 67's: nine proofs recorded, four re-run independently in QA and all four held — while **thirteen fail-open routes sat in the shipped classifier**. **The review is what made this shippable, and it caught a stale premise the task could not have known it had.** The task was written against a 96-line, four-shape version of the target; `develop` had grown it to 140 lines with five shapes in between, invalidating six statements and two success criteria — including a ~160-line cap that had silently become unmeetable, allowing 20 lines for content needing 55. The cap was re-derived as a **delta** (~55 added lines) rather than an absolute, so it survives the next drift instead of expiring again. The general lesson: **a scope written as a list of additions never prompts anyone to ask what those additions invalidate** — the stale line is the one that did not move, so no diff shows it. **The review also stopped the document violating itself.** The task instructed writing a bare "**9 proofs held**"; the source says nine were *recorded* and four *independently re-run*. That shorthand would have been an overclaim written into the one document whose closing section is *do not claim it unless you did it*, and whose own worked example of that failure is a commit message claiming proofs nobody ran. **QA's one MEDIUM was the same shape as the review's finding**: the change grew the file 39% and left the frontmatter `description` — which `coding-standards.md` calls "the most-read line of any skill, written for the matching agent" — describing only the original question, so the two new sections were undiscoverable by description match. Fixed in one line, 60 words, keeping the original procedure sentence first so every existing match still lands. **Cycle 2's refute pass found no false claim and said what it searched** rather than reporting a bare "None" — including one candidate probed at length and dismissed on the evidence (the boundary paragraph's closing "nothing above asks you to keep one" reads as contradicting the row above it, but the row asks you to *prove both directions* while the sentence observes that nothing asks you to *maintain a standing accept-set*, which is the stronger obligation that actually caught the task-67 regressions). The four mandated lifecycle transitions were recorded **not applicable** rather than performed as ritual — the change set is markdown, so there is no state for a transition to be wrong in. **Mutation proving is n/a and was recorded as such at every gate rather than fabricated**, which is exactly the behaviour the changed document mandates. Two LOW observations ship open, both pre-existing and neither introduced here: the file's own fenced `bash` block cannot be executed by qa-task Step 4b because `cp` is off the fail-closed allow-list — so the mutation-proving procedure's own worked example is the one snippet QA cannot verify — and `skills/develop/SKILL.md` still says "the four shapes" against a five-shape document, which §4 Out of Scope forbids fixing here. **Two process notes.** The `/finalise` CI gate did real work: the rollup read `PENDING` on first sample with `test` `IN_PROGRESS` and `conclusion: ""`, the exact state a `.conclusion // .state` read reports as green; it was waited on, not assumed. And the merge gate failed once on `access-config-parity`, diagnosed rather than retried blindly — the branch touches nothing that suite covers, `ci:fast` had passed it twice on this branch, GitHub CI was green on the same commit, and the failing test ran **2.2s alone against 13.2s under load**; the identical commit went green on re-run. Same contention flake this roadmap already records against T72. QA PASS 100/100 over 2 cycles, DoD PASS, CI 4/4. Frontier advances to **T70** |
| 2026-09-02 | **T70 accepted — PR #308 merged**. `/review-code --comment` documented "post each finding as an inline review comment at its `file_line`" since it was written, and **no code implemented it** — no `pulls/*/comments` or `pulls/*/reviews` call existed anywhere in the tree, while `/review-pr` scoped the behaviour out and named this task as the reason. `shared/resources/pr-inline-comment.js` is now the peer of `tracker-comment.js` one axis over: that CLI comments on a tracker **issue** and branches on `$TRACKER`, this one on a pull **request** and branches on `$VCS`. Registering `bitbucket.pr.comment` was not scope creep but a prerequisite — `defer-mutation.js` **throws** on an unregistered kind, and the roster had no Bitbucket kinds at all, so the Bitbucket arm could not have honoured the `ACCESS_TRACKER` gate the task requires (roster 23 → 24, plus renderer, verifier, fixture and totality assertions). **The item is really about what two QA cycles found in a module that development left 40/40 green.** Its one stated invariant is *a finding is never dropped*, and it dropped findings on two reachable paths: the duplicate-marker branch recorded `unverifiable` and returned **without** degrading — no inline comment, no summary, and its warning printed the key but never the body — while the branch immediately above it, handling the same situation, degraded correctly; and the derived id was the **anchor alone**, so two findings on one line (a null check and a missing await on the same call) shared an identity, overwrote each other on re-run, and both reported `updated`. Neither was visible to the author, who knew what he meant. **Three of cycle 1's four worst findings came from the independent reviewer**, which verified two by *executing* `runGithub` rather than reading it. The wiring was worse than the module: both jq snippets read `body: `.summary`` — a key the findings schema does not define — and iterated `.code_review[]`, the wrapper's *values*, so jq aborted outright and **every finding was dropped, not degraded**. Written from memory of a schema one grep away, into the very task that exists because a skill documented behaviour it did not have. **Cycle 2 ran as a refute pass over the whole diff and this is where the rule earned itself twice.** Two of its eight findings were in the **original commit** and invisible to cycle 1 — `gh api --paginate` emits one JSON document *per page* (its own `--help` says so; without `--slurp` any PR past 30 comments silently reverted the module to the summary-only behaviour it was built to replace), and the jq was fail-open in the wrong direction, aborting the entire array on one `file_line` of `src/x.ts:42-58`. A cycle-2 narrowed to files-changed-since-the-last-gate would have read only cycle 1's repairs and passed clean. **And cycle 1's fixes created two of cycle 2's defects**: making every `unverifiable` site also degrade was correct and silently rendered a neighbouring branch unreachable (`--strict` could no longer report the one condition it exists for, green throughout because nothing covered it); and the stale-anchor check landed on the GitHub arm only, after which a contract paragraph was written *excusing* the gap — "Bitbucket has no equivalent of `position: null`" — which is true and beside the point, since the comparisons that fire are path and line and Bitbucket returns `inline.path` on every inline comment. A limitation documented instead of noticed. **The durable output is a guard, not two corrections.** The jq defects were invisible to every existing check — `qa-execute-snippets` skips those blocks as `mutating`, and a reader sees plausible jq — so both skill suites now **extract the program from SKILL.md and execute it against a schema-shaped fixture**, with malformed rows (a range, a bare path, a missing `suggested_action`) proving the well-formed finding survives its neighbours. The cycle-1 version of that fixture *invented* `file_line` on conformance findings, which use `ref`, so it tested a shape production never emits and would have passed with the conformance arm entirely broken. 17 findings, 16 closed, 1 open by decision; **7 mutation proofs**, each turning exactly its own assertion red; suite 40 → 47 unit plus 65 skill-contract. `/finalise`'s probe mode fired — this deliverable *is* a boundary — and **29 candidates executed against the shipped validators with 0 reproduced**, including the cycle-1 and cycle-2 collision defects re-probed from outside rather than trusted from their unit tests. Accepted at Maintainability **CONCERNS** rather than PASS: the two arms carry near-duplicate partition ladders, which is the direct cause of the Bitbucket stale-anchor gap above, and extracting one `partitionFindings()` is named as follow-up rather than done at the end of a fix cycle. Two further limitations recorded rather than buried — the summary comment has no marker of its own so repeated runs append, and **the Bitbucket arm has never run against Bitbucket**. QA PASS 92/100 over 2 cycles, DoD PASS, CI 4/4, quality gate `npm run ci` green. **The task carries no tracker issue** — flagged Important at review and still open; run `/sync-github-task`. Frontier advances to **T77** |
| 2026-09-04 | **T77 accepted — PR #309 merged** (`5a6664c`). `/review-pr`'s conformance lens is now **Step 5c**, the exit gate of the Steps 5–6 QA loop: a `PASS`/`WAIVED` gate no longer leaves the loop on its own, `REQUEST CHANGES` routes back to `/qa-fix` on the *same* 5-cycle budget, and `ready-for-merge` moved behind the review. Still 8 steps; `/review-pr` gains no gate-writing power. **Accepted by operator waiver, not by a PASS** — the run hit Loop Escalation at cycle 5 and then took **seven independent gates** (4 FAIL, 3 CONCERNS) and **three DoD runs**, none reaching PASS. Every finding across all of them was in the artifact trail or in test strength; none was a defect in pipeline behaviour. The task caught its own class of defect on its own PR — including a fabricated mutation proof, and **six instances of one bug class**: an assertion claiming a *relationship* (X routes to Y) that only tests whether both names appear, twice recurring inside the fix for the previous instance. A lint for prose-matching assertions is the durable lesson. |
| 2026-09-04 | **T89 accepted — PR #312 merged** (`07edf92`). Task 77's retrospective named a bug class it hit **six times across eleven gates**: an assertion claiming a *relationship* — X routes to Y, X fires at Y, X owns Y — while establishing only that both names occur in the same slice of prose. Every one passed against the mutation it was written to catch, and **two of the six were written inside the fix for the previous one**, because widening the regex is the natural repair and is also the defect. `tests/relationship-assertion-lint.test.js` now makes the class a CI failure instead of a reviewer's lucky catch, over a pure analyser at `tests/lib/`. It is **structural, not textual** — a state machine that understands strings, regex literals and comments, reasoning about each assertion's pattern and message separately, because a lint against co-occurrence matching implemented *as* a co-occurrence match would be self-defeating. Wired in by placement: `tests/*.test.js` is already globbed, so there is no `package.json` entry to keep in step. **It found six live instances on its first clean run**, three of them residual copies of the very assertions task 77 spent eleven gates fixing, still standing beside their own `verdictRow()` replacements — harmless in effect, but a reader auditing that file found an assertion whose message said it checked the row when it did not. All six were fixed, not suppressed. **The conjunction is what makes it usable.** The first implementation required only the defective shape and reported **61 findings over 2188 call sites** — mostly ordinary presence checks like `/current_step/`. That is the rate at which, in this repo's own words, *a guard that cries wolf gets disabled*. Every rule now requires the shape **and** a message claiming placement or a mapping: 61 → 11 → 4 suppressed → **0 unsuppressed**, each suppression carrying a written reason, with a bare marker rejected and that rejection mutation-proved. **Rule D was added during implementation and disclosed rather than folded in silently** — instance 4 is an *omission* (4 of 5 values enumerated, the missing one being the loop's exit arm), not a pattern shape, so criterion 1 was unmeetable with the three filed rules. **Three self-catches, all disclosed rather than quietly corrected, and they are the item's real content.** A **false mutation proof**: M5 first reported green because shell escaping had swallowed the substitution, so the "mutation" tested nothing — caught by checking the file rather than trusting the result, and recorded in the FP README because `mutation-proving.md` exists for exactly that. **Two analyser bugs found by measuring rather than reading**: `identifierRuns` required a leading letter, so `5b` — the destination in the defect this lint was built from — was not counted, and **instance 3 was invisible to the rule written for it**; and `owns?` matched the possessive "its own provenance header". And sharpest, **a defect in the fix's own guard, found by mutation-proving that guard**. QA cycle 1 found CY1-1 (the scanner rejected `>`, so a regex after `=>` was scanned as code and an odd quote count opened a phantom string that ran to EOF, silently hiding every later assertion — 0 of 89 files affected, so the *mode* was the defect, not its cost). The fix's first guard put all seven value-position shapes in one probe file, where the apostrophe in `return /it's/` was closed by a *later* line, re-syncing the mask and leaving the keyword arm vouched for by nothing — **instance 2 and instance 6 of this task's own corpus, one level down, inside the fix for the finding that named the class**. Shapes are now asserted one at a time. **Nine mutation proofs, and M13 is the one that carries the gate**: reverting `>` *and* injecting a real odd-quote line into a live corpus file turns the corpus reachability sweep red and makes it name that file — which is what separates a live guard from one that merely happens to pass, since a sweep over a corpus that currently triggers nothing is otherwise indistinguishable from a sweep that cannot fire. Step 5c returned CONCERNS on the last finding of the run, and it was the same shape as everything above: every published number was a snapshot at `fe7f617` that the fix had moved, anchored to no commit — an *under*claim, fixed anyway, because this is the task that exists to stop an assertion claiming more than it establishes and an unanchored number is that failure with the sign flipped. `mutation-proving.md` gains the class as **shape 6 of six**, with the standing caveat that the lint models the six shapes that happened and a seventh in an unmodelled shape will pass. One LOW ships open by decision (the keyword arm does not exclude property access — 0 occurrences, bounded to one line, and non-silent because the guard names it). **No tracker issue exists and that is recorded rather than skipped silently** — sync is opt-in and no operator was present to consent. QA PASS 100/100 over 2 cycles, DoD PASS, CI 4/4 on the final head, merge gate `npm run ci` green (2320 tests). Frontier advances to **T90** |
| 2026-09-04 | **T90 accepted — PR #313 merged**. The lock file is the develop pipelines' own state machine — every `develop-*` orchestrator advances it as the last action of each step, and both hooks read `current_step` from it to decide where a compacted run resumes. Given a **zero-byte** lock it printed `step 0 → 5`, exited **0**, and left the file empty: **success reported for a transition that never happened**. The cause is a property of `jq` that is silent in both directions — on empty input it emits nothing and exits 0 — so the defensive read fell back to 0 and, the actual hole, the `if ! jq … > "$LOCK.tmp"` write guard did not fire either, and `mv` installed the empty file. A **whitespace-only** lock was worse: the same path *truncated* a file that had content and still reported success. Also fixed: the `> "$LOCK.tmp"` redirect **followed a pre-existing symlink** on a predictable path, now `mktemp` in the lock's own directory (`O_EXCL`, unpredictable name — `noclobber` was rejected as weaker, since a symlink to a *non-existent* target is still created through it). **The task was filed on a claim that turned out to be false, and QA caught it.** §2 asserted that 18 malformed `current_step` values "all correctly preserve the lock and exit non-zero", making the empty file *the single hole in an otherwise well-behaved validator* — inherited from task 77's DoD probe and carried forward unverified. Executed, **6 of the 8 values it named advance and exit 0**. What actually held is simpler: the script failed closed on input `jq` could not *parse* and advanced on everything else — which is why both the empty file **and a bare `null`** slipped through. That `null` case was a live instance of the identical defect, fabricating `{"current_step":5}` from a file that never held an object; it was **fixed rather than deferred**, widening the guard from "empty or whitespace-only" to "not a JSON object" with §4 and §9 updated to match. **Fixing it surfaced a fourth defect, in the fix.** Appending the type check after the existing emptiness check left the suite green while the original mutation proof had **silently stopped holding** — neutering the emptiness branch kept all 30 tests passing, because the type predicate already covers empty input. That branch had become control flow no test could falsify, introduced by a fix rather than by the original work, and was caught by *running* the proof rather than assuming it survived. Resolved by restructuring to **one decision predicate**, with the emptiness test demoted to choosing the error message. **`--complete` is deliberately exempt** — it removes the lock without parsing it, and gating it would make a corrupt lock permanently unclearable; pinned by its own test. New `pipeline-lock` legend tag, since no tag covered this script and `--batch` could have paired T90 with another row editing the same file. **Three self-inflicted defects are kept in the record rather than tidied away.** A **false finding** that `npm run bundle` was leaving copies stale, from a checksum comparison that could never match — a bundled copy is the source *plus* an `AUTO-GENERATED` banner line — compounded by reading only the last fifteen lines of a ~130-line bundler output; retracted with both causes named, and explicitly declining to claim it had reproduced `task.86`. Acting on it briefly stripped that banner from 9 files; the pre-commit bundle restored it. A **28 MB corrupted implementation report** — 480,884 lines, one paragraph repeated 12,326 times, from `str.replace("", …)` on a reversed Python slice — **committed and pushed before QA caught it**, rebuilt to 218 lines. And the vacuous-branch defect above. **The most useful finding is what could not see any of it**: `prettier --check` passes on a 28 MB well-formed markdown file, and `npm run ci` had already run before the corruption existed, so **no gate in this repository can see a committed artifact orders of magnitude larger than plausible**. That follow-up is recorded and **owned by nobody yet**. Step 5c returned CONCERNS on two conformance findings — a criterion still asserting the retracted bundler claim (found in **two** places; the review named one), and a `npm run ci` tick carried from a pre-`a1e836a` run — both closed before finalise. 30 tests green under **bash and zsh**, both fixes mutation-proved individually, 24 security probes executed with 23 held (one pre-existing, cosmetic; one *retracted as a wrong expectation* rather than reported), 9 bundled copies content-verified. Four LOW residuals and the partly-declarative scenario 12 (`[]`, `"str"`, `42` are asserted but not mutation-proved — only `null` binds the predicate) carried honestly. No tracker issue exists and that is recorded rather than skipped silently. Frontier advances to **T78** |
| 2026-09-04 | **T78 accepted — PR #314 merged**. The last of the three pipelines that could still commit an unformatted tree now cannot. T75 put the fast gate in the develop loop and each `qa-fix` cycle; `develop-bug` shares the first document and got that half free, but its per-cycle **verify** loop is a separate file that got nothing — so a bug fix cycle could close green, push, and fail CI on a file it had just rewritten. That is the task-67 failure, still live for bug fixes only, on the run least able to afford a round trip through red CI. The gate now sits at step **3a** of `5b`, after the no-change check that HALTs and before the commit — structurally where `qa-fix`'s `0a` sits, so **TASK-75-001** (gate placed *before* the no-change check) is avoided by construction rather than by care. The retry bound is stated as 2 attempts with `MAX_ITER` described as bounding *cycles* and explicitly not this inner retry, which is the wording T75's own QA corrected. **Why it was missed is the durable part, and the task reproduced it.** The other two loop documents live in `shared/resources/`; this one is skill-native under `skills/develop-bug/references/` with no shared source, so a file list drawn from `shared/resources/` cannot see it — and **every path in the task document named a `shared/resources/` path that does not exist**, so the fix was about to repeat the mistake it was fixing. `/review-task` caught that as its one Critical finding (9/10, 5 of 6 recommendations applied). `ci-gate-parity.test.mjs` now iterates all three documents at their real sources with a `length === 3` tripwire, **mutation-proved on each**: strip the gate from any one and the test goes red. **QA cycle 1 found three MEDIUM, two of them port artifacts** in a task whose §3 warned "this is not a copy-paste" about the gate's *placement* — the warning applied equally to its prose: a `step-3` reference that is unambiguous in the source document and collides here (5b *has* a step 3, the no-change check), and a failure instruction pointing at a template with no field for it. The third was a doc sweep the plan omitted — **three** live sites still said two, and `ci-gate-parity` already *reads* `configuration.md` but asserts only the default value, so it opened the file and passed over the stale prose. **The fix cycle caught a fourth defect in its own fix**: the `Fast gate` field was first added to the tracker-comment template too, but that comment is a single POST at the end of 5a, *before* 5b runs the gate — the same defect inverted, a slot with no value rather than a value with no slot. Reverted, with the asymmetry documented so restoring it means arguing with a note. Cycle 2's unscoped refute pass found nothing further; both port fixes now carry *why* they are as they are. Step 5c returned CONCERNS on one finding — §7 Files Summary listed 3 files where 5 changed — closed before finalise. **The gate governed its own delivery**: `ci:fast` ran before the qa-fix commit, and finalise waited on a PENDING rollup rather than rounding it up. 2 QA cycles (CONCERNS 80 → **PASS 100**), DoD accepted, `npm run ci` green. **Two items recorded as out of scope, not deferred**: the Step 4b runnable-prose rule scopes to `SKILL.md` and `shared/resources/*.md` and so cannot fire on a skill-native `references/*.md` — this task's own blind spot one layer up — and the uncommitted-fix handover on a fifth-cycle twice-red gate, which is **identical to the qa-fix loop**, so changing it here alone would break the parity T78 exists to establish. No tracker issue exists; recorded rather than skipped silently. Frontier advances to **T83** |
| 2026-09-05 | **T84 accepted — PR #318 merged** (`6db2581f`). Install profiles: `minimal` 5 / `pipeline` 36 / `full` 109 skills, **−60%** of the description budget for `pipeline` (35,425 → 14,281 bytes) — the tier paid on every request before the agent reads an instruction. **The specified design was replaced mid-implementation**: §3's prose-scrape of `/slash-command` tokens was built, measured, and collapsed `minimal` and `pipeline` to the same ~34 of 120 skills — worthless, while reporting success. Prose carries no edge direction; `review-code/SKILL.md:180` reads "`/develop-story` and `/develop-task` do **not** call `/review-code`" and the scrape read it as two edges. Now declared in an `invokes:` frontmatter key on 20 skills, absent = no edges. **27 defects over 4 passes, each pass finding the previous pass's fixes defective** (11 → 5 → 5 → 10). Four reachable configs silently installed every skill. The worst: a bare `X=$(cmd)` assignment aborting the wizard under errexit, **added in the same commit as a comment warning against it**, unseeable by its own test because that test ran under `set +e`. **The two transferable findings are about method, not code.** Tests asserting on source text caught **0 of 27** defects and twice gave false confidence about the exact line one sat on — one asserted on a *code comment*, which cannot fail. And **recording a check as unrunnable is a claim**: `shellcheck` was written off across three gates as unavailable; docker was there all along, T83's own entry documents using it, and it immediately found a new SC2155 in this change. The real `--update` was likewise called impractical, then run against a full 120-skill install — 120 → 120, nothing pruned, confirming the tracker and profile keep-paths by execution rather than by reading the source. Frontier advances past T84 |
| 2026-09-05 | **T91 accepted — PR #320 merged**. Install-time and run-time tracker resolution were two implementations that disagreed on three config shapes; `setup-consumer.sh` now **delegates** to `resolve-platform.sh` in a subshell and its local awk parser is gone. **A and B, not A or B**: the task framed three exclusive options, but they fix different rows — `tracker: bitbucket` and `tracker:<TAB>jira` are config-*parsing* divergences, while the `.env` case is a *source* divergence that delegation alone cannot fix, because delegating wholesale deletes the installer's `.env` probe and becomes the rejected Option C. So the runtime resolver learned to read `.env` (below env, below config) and delegation then preserves the probe. ⚠️ Behaviour change for every skill: no `tracker:` key + a `JIRA_URL` in `.env` now resolves `jira`; opt out with an explicit `tracker: github`, which no wizard-generated config needs since T83 writes one. **21 findings over 4 gates, and the first two cycles shipped fixes that no test executed.** Cycle 1's fix for the empty-`TRACKER` case was *unreachable*: `printf "%s\n%s"` loses its trailing newline to command substitution, so both halves of the split returned `"0"` and the installer resolved the literal string `"0"` as a tracker — matching no classification list, leaving the filter silently inert, with the suite green at 2441 tests the whole time. **The gap was in the fixture, not the code**: `makeFixtureTarball` shipped no resolver, so every install test resolved through this repo's own checkout and the `release` origin — the copy a real consumer installs with — was exercised by nothing. **`/review-pr` found two more the loop had not.** The PR did not contain the PASS evidence it was judged on (gate.4, qa.3 and qa.4 untracked; anyone opening it read `FAIL 70/100`), and `platform-detection.md` — the file §7 designates the source of truth and requires to change *first* — sat two commits behind the resolver it governs, asserting a `^JIRA_URL=.+` pattern neither side used any more, propagated into all 38 bundled copies. Four QA cycles interrogated the installer's error contract exhaustively and never once re-read the canonical spec against the code they were changing. **The lesson to carry**: a green suite was never evidence here. Every finding came from executing the code against hostile input, and a fix no test runs is a claim rather than a guarantee. Tests 40 → 61 in the parity file, plus 8 cases in the runtime resolver's own suite — which §8 named as the regression net and which nothing had extended. Frontier advances past T91 |
| 2026-09-04 | **T83 accepted — PR #315 merged** (`816330e`). Seventeen shipped skills are tracker-specific and mutually exclusive — 11 Jira-only, 6 GitHub-only — and every consumer received all seventeen. The cost worth fixing is **mis-selection, not disk**: auto-activation matches on `description`, the two siblings differ there only in the platform noun, and because `resolve-platform.sh` is sourced *inside* the skill the wrong pick is not a clean error at the top — the run gets some distance in first. The context saving is real but secondary (~1,505 of ~11,702 tokens, 12.9%, recomputed in QA against the CHANGELOG's ~13% claim). **The interesting part is what the QA loop caught.** The change arrived with 2,343 tests green and seven developer mutation proofs, and still had a defect that **inverted the feature**: Phase 1 said *mirror `resolve-platform.sh`, do not re-derive it*, and the order was mirrored faithfully while the **value parsing** was re-derived as `awk '{print $2}'` against an `[a-z]` pattern. A quoted `tracker: "jira"` — idiomatic YAML — did not match the pattern *at all*, and a CRLF checkout left `jira\r` on the token; both fell to the `github` default while the runtime reader parsed them as `jira`. **A Jira repo installed with none of its eleven Jira skills, silently**, the failure surfacing days later inside a pipeline step, and the grandfather rule cannot help because a fresh install is exactly the case the filter applies to. It was found by **executing both resolvers over a table of legal spellings** rather than reading the code, then reproduced end-to-end against a fixture tarball — the reading-based reviews before it, including a 9/10 review report, had walked past it. Cycle 2's refute pass then found the follow-on defect **in cycle 1's own fix**: the new parity helper scrubbed two env vars but not `SKILLS_CONFIG_FILE`, which `resolve-platform.sh` honours as an override of *which file to read*, so an ambient value could flip the very guard protecting the HIGH fix — and `callFn` names that exact hazard in its own comment, so the list had been copied and not extended. The fix consolidated **three** copies (a third nobody had reported) into one `hermeticEnv()`. Cycle 3 verified both closures by re-running the checks that produced them, including the suite re-run with the polluting variables *genuinely exported*. **QA also found a defect in its own output**: gate 2 carried a closed HIGH entry forward "so the gate carries the history", which feeds the third-strike detector a false second strike — that rule deliberately ignores `status: closed` — and a third would have tripped "replace, do not patch" on a healthy file. 3 cycles (FAIL 70 → CONCERNS 80 → **PASS 95**), 8 QA mutation proofs on top of the developer's 7, 3 bugs filed and closed. Step 5c returned CONCERNS on three findings, all actioned before finalise. **`shellcheck` was closed, not waived**: QA escalated it because the binary is absent here *and* no workflow runs it, so no fix cycle could tick it; Step 7 ran the official container against both the baseline and the branch — 1 warning each, the same pre-existing `SC2209` in code the diff never touches, **0 new**. DoD security probe mode fired on the install classifier: **14 candidates executed, 0 reproduced**. **One residual ships deliberately** — a repo with no `tracker:` key whose `JIRA_URL` is only in `.env` still resolves differently at the two ends; bounded, tested, documented, escapable via `--all-skills`, and its proper close changes every skill's resolution, so it needs its own task. **That follow-up is un-filed**, as is a tracker issue for this task — it has none, so every tracker signal this run was skipped. Frontier advances to **T84**, which builds on this filter |
| 2026-09-05 | **T92 accepted — PR #322 merged**. The repo's least-gated language now has a lane: `--severity=warning` over the **56 tracked source** shell scripts (not the 247 `git ls-files` returns — 191 are bundled copies, and linting them reports every shared finding four or five times), ShellCheck **pinned to v0.11.0** and printed, in its own unfiltered workflow. T83 is why: its `shellcheck scripts/setup-consumer.sh` criterion rode three QA cycles, a gate and a DoD with nothing able to evaluate it. **The task's own recommendation would have produced a lane that could not fail.** §6 named `validate.yml`, which is path-filtered to `skills/**` and `shared/resources/**` — excluding `scripts/setup-consumer.sh`, `scripts/release.sh` and `.agents/scripts/backfill-story-issues.sh`, all three carrying a warning, the first being *the script that motivated the task*. `test.yml` is unfiltered but guarded by `ci-gate-parity.test.mjs`, so a separate one-job workflow it is. The gate was **mutation-proved three ways** — a deliberate finding sited deliberately in `setup-consumer.sh` so the proof exercises the region the original design would have missed, a widened glob at 247 files, and an empty list — each reverted and the clean tree re-confirmed. **All 26 pre-existing warnings resolved: 9 by a real fix, 17 by a reasoned disable, zero bare suppressions left.** Two were **not false positives**: the SC2211 backticks sat inside a *double-quoted* `assert_rc` message, so bash was executing `? access` as a command substitution and silently stripping the emphasis from what the assertion printed. Three SC2034s were **genuinely dead** — `EPIC`, `TASK_ID`, `VALID_TYPES_RE`, the last having already drifted from the inline regex it duplicated, so wiring it up would have been a behaviour change and deleting the stale copy was the honest fix. **Every one of the five findings was in code this task introduced; the inherited tree was clean at `error`.** The sharpest was added *while fixing the SC2010*: an empty-list guard that reported a failure and then **continued**, into the exact `sed`-reads-STDIN hang its own comment claimed to prevent — `return` at the top level of an executed script is illegal, and `2>/dev/null || true` swallowed both the error and the status. **The task.90 shape, reproduced inside the task built to catch it.** Unreachable in practice, so the suite stayed green throughout and would have stayed green forever; it was found by mutation-proving the guard rather than by running anything. The other three were numbers that had been true once and silently were not any more — **11/15 where the real split is 9/17** (counting *assignments quoted* rather than *findings resolved*), **"five workflows" where there are six**, and a sources-only comment still asserting **"725 vs 81"** in the present tense when this very change had made it 515 vs 55. A green suite was never evidence here. ⚠️ **Independent review never happened, and that is recorded rather than smoothed over.** Three Explore subagents — the QA cycle-2 refute pass and both Step 5c lenses — hung without output, ignored wrap-up requests and were terminated, one after roughly forty minutes; finalise's four parallel DoD agents were not dispatched on that evidence and those sections were verified in-line with executed commands instead. Self-review still found four of the five findings, so it was not worthless, but the mechanism designed to catch what self-review misses was never exercised on this branch and neither the PASS gate nor the acceptance should be read as evidence that it was. **A human read of the 28-file reviewable surface is the outstanding mitigation**; the other 137 files are generated and confined to `d383fa90`. **The subagent failure is itself un-filed and owned by nobody.** Migration consequence, announced in the CHANGELOG rather than discovered: the lane fails any PR adding a warning-tier finding, including in a file the author did not know was watched. Frontier advances past T92 |
| 2026-09-05 | **B6 accepted — PR #323 merged** (`c1ce3388`). The third generation of the snippet classifier's fail-open defect (13 → 14 → 13 routes), closed at **four root causes** rather than thirteen patches: `commandWords()` now scans every command in a segment instead of stopping at its first token, `WRITE_REDIRECT`'s pre-operator class no longer excludes word characters, one quote-state-aware walker replaces two mutually-blind regexes, and `-o` / sed's `w` resolve against the command they belong to. **The lesson is about the corpus, not the classifier.** Thirteen pinned inputs were fully green while the fix's own first cut carried **two brand-new fail-open routes** — a command substitution inheriting the enclosing segment's `-o` exemption (`find . -newer $(sort -o /tmp/pwned f)` → runnable), and `gitSubcommand()` reading the segment's second token rather than the tokens after *this* `git`, so a `case` arm resolved to the allow-listed `git:log` and licensed a `git checkout`. A corpus answers "did the reported bug go away?"; it does not answer "what did this change make reachable?" On a fail-open defect those are different questions, and only an adversarial review of the diff asked the second. Two verify cycles, five defects found in the fix itself, all closed and pinned. Also recorded: **a mutation proof reported green because its edit silently matched nothing** — re-run with an assertion that the file actually changed, it went red, which is the second time this repository has hit that trap. The report's own guidance was wrong in two places and is corrected in place: it recorded `elif` as safe (it is not, nor are `for`/`case`/`esac`/`done`/`fi`/`function`), and its suggested fix — extend the splitter with three keyword names — would have left that whole family open. One gap is left open deliberately and recorded: a glued `sed 's/a/b/wfile'` still reaches `runnable`, because every regex tried for it false-positives on `sed 's/warning/x/'`; it needs a delimiter-aware parse, not another pattern |
| 2026-09-06 | **B11 accepted — PR #329 merged** (`f560cc21`). The durable half of bug.11: `sync-jira-*` had **no way to be status-neutral**, so finalise's Document-link re-point was unavoidably a *second* status resolver running after the `tracker-workflow.yaml` ladder had already decided — and on a consumer mapping `accepted` to a non-terminal column it walked the card back out of the terminal status, stranding the resolution. PR #326 shipped the doc mitigation; this removes the second resolver. `--no-transition` is gated **inside** `syncDocumentStatus` (returns before any HTTP, `reason: "transition-suppressed"`), so it holds for all four call sites rather than for whichever callers were remembered — `sync-jira-epic`'s no-field-changes path was in fact missed on the first pass while every behavioural test still passed. Selected via the **bug-registry fallback**, so there is no roadmap row to tick; registry row 11 is `closed`. 19 regression tests, six mutations proven. Three review rounds found 13 findings, 9 of them defects the *fix* introduced — a tautological test cited as evidence, a fixture that kept it blind to gate deletion, and a rationale that was backwards; all corrected. One finding was out of scope and is carried as **bug.12** (three `review-*` body-only syncs with the same shape). |
| 2026-09-06 | **B12 accepted — PR #330 merged** (`70860c83`). bug.11's remainder, at the three call sites it deliberately left: `review-story` 9.6, `review-task` 8.6 and `review-epic` 11.5 all describe themselves as body/link-only syncs and all still re-resolved status through `loadStatusMap` after the ladder had placed the card — reachable from Step 2 of both standard pipelines, walking the card back out of In Progress. The defect class was **enumeration, not logic**: the flag worked, every call was correct in isolation, and bug.11's own tests were green, so no behavioural test could have found it. The deliverable is therefore the **population check**, not the three one-word edits — tests G/G2/G3 assert that every writing `sync-jira-*` invocation in shipped prose either passes `--no-transition` or sits in a deliberate-status-push allowlist, with G2 failing on a stale allowlist entry and a `>= 10` floor so a scan that stops matching fails rather than passing vacuously. Verify Cycle 1 then found a defect **in the guard**: a naive fence toggle that, on a nested ```` fence, collapsed Step 9.6 and Step 10 onto one heading — allowlisting the deliberate push would have silently allowlisted the body-only sync beside it, reinstating this exact bug behind a green tick. Fixed with CommonMark fence semantics + test G4. Six mutations proven; **two mutation attempts rejected as invalid first** (one broke the file's syntax, one used an even-count fence the toggle recovers from). The full-PR adversarial review never returned and is recorded as a gap rather than smoothed over. Selected via the **bug-registry fallback**, so there is no roadmap row to tick; registry row 12 is `closed`. |
| 2026-09-06 | **B7 accepted — PR #331 merged** (`da95627e`). The Step 4b anti-vacuity guard used one condition — `blocks.length > 0 && counts.runnable === 0` — for **two states**: an under-configured run, which `--bind`/`--copy` fixes, and a file whose every documented command is deny-listed by design, which nothing fixes. Six of ten skills surveyed sat permanently in the second, three of them receiving a bare finding with *no* remediation, because the `--bind` hint is appended only when there is a placeholder to bind. A check that fires on most of the library with no available fix is one reviewers learn to scroll past. Split on `counts.placeholder`: the finding survives for the actionable case (now always carrying the remedy), and the refused case emits an informational `no-executable-blocks` record into a new `notes[]` — recorded, never a finding, never a silent no-op. **Two things beyond the report's own suggested fix, both load-bearing**: the split reaches the **exit code** (`1` → `0`), because a reporting split that stops at the payload never reaches callers that read status; and the note carries a **per-reason refusal breakdown**, because collapsing `unrecognised-command (fail-closed)` into "correctly refused" would have hidden the exact over-refusal signal B6 and B10 were about — the reproducing `commit-changes` file turns out to be *entirely* fail-closed. Four regression tests, three red on the pre-fix branch; the fourth stays green **by design** as the over-correction guard, so "delete the guard" cannot pass in place of "split the guard". The parity eval had pinned the *old* heading text and would have kept passing against a doc still giving the wrong instruction — replaced with assertions on the split itself. This is the third instance of one-signal-two-states in this neighbourhood after T73's `probes: []` and B8's bug status: worth recognising by **shape**. Selected via the **bug-registry fallback**, so there is no roadmap row to tick; registry row 7 is `closed`. |

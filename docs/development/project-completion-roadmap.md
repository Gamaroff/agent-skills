# Project Completion Roadmap

How to use this document: work top to bottom. Only `[ ]` rows are candidates; `[x]` rows satisfy
`deps:`. This is a **living backlog** — completed items are archived to
[`roadmap-history.md`](./roadmap-history.md), so a `deps:` entry naming no current row means
_already shipped_, not an error. Resolve any such reference there.

**Phases 1, 2, 3 and 4 are complete and archived** to
[`roadmap-history.md`](./roadmap-history.md) — resolve any `deps:` naming a row you cannot find here
against that file. The rows under "Deferred / human-gated" remain invisible to selection.

**There is no standing maintenance phase, and there should not be one.** Phase 4 was exactly that —
a hand-maintained list of outstanding work, kept so the loop always had a frontier — and it is
retired. When no phase holds an actionable row, `select-next.mjs` now falls through to
[`docs/bugs/bug-registry.md`](../bugs/bug-registry.md) and
[`docs/tasks/task-registry.md`](../tasks/task-registry.md) directly, so a filed bug or task is visible
to `/develop-next` **without anyone transcribing a row here**. A task is in that frontier at
`ready-for-development` or later; a bug at `new` or `reopened` — promotion up the item's own ladder is
the opt-in, and its document's frontmatter decides, not the registry row. Add a phase here only to
express *sequencing* the registries cannot: a deliberate order, a dependency chain, a human gate.
Rules: [`roadmap-selection.md`](../../skills/develop-next/references/roadmap-selection.md)
§"Registry fallback frontier".

Selection is executed by `select-next.mjs`, never by eye:

```bash
node .agents/skills/develop-next/scripts/select-next.mjs --lint      # format check
node .agents/skills/develop-next/scripts/select-next.mjs             # what's next
node .agents/skills/develop-next/scripts/select-next.mjs --batch     # what N agents can do at once
```

Marker vocabulary and the full algorithm: `skills/develop-next/references/roadmap-selection.md`.

## Legend — conflict footprint (`touches:` tags)

Read only by `--batch`. Two rows conflict when they share a tag that **either** side marks `!`
(hard/exclusive); shared `~` (soft) tags are tolerated on a second-merger-rebases basis.

| Tag                | Covers                                                                 |
| ------------------ | ---------------------------------------------------------------------- |
| `jira-sync`        | `shared/resources/jira-sync.js`, `jira-stage.js`                       |
| `gh-stage`         | `shared/resources/gh-stage.js`                                         |
| `workflow-engine`  | `shared/resources/tracker-workflow.js`, `yaml-subset.js`               |
| `pipeline-steps`   | `shared/resources/develop-pipeline-step-*.md`, `lite-mode.md`          |
| `orchestrators`    | `skills/develop-next/SKILL.md`, `skills/develop-batch/SKILL.md`        |
| `setup-consumer`   | `scripts/setup-consumer.sh`                                            |
| `docs-config`      | `docs/reference/configuration.md`, `docs/reference/tracker-workflow.md` |
| `bundles`          | `skills/*/references/*` (regenerated by `npm run bundle`)               |
| `change-log`       | `shared/resources/change-log.js`, `document-change-log.md`              |
| `templates`        | `skills/*/resources/*template*`, `docs/templates/*`, `create-*/SKILL.md` |
| `review-skills`    | `skills/review-*/SKILL.md`, `skills/edit-*/SKILL.md`                    |
| `loop-supervisor`  | `skills/loop-supervisor/*`, `evals/loop-supervisor/*`                  |
| `test-harness`     | `package.json` test script, `shared/resources/tests/*`                  |

---

## Deferred / human-gated

Rows here are invisible to selection. Move a row up into a phase when it becomes actionable.

- [ ] **T41-fixtures** Capture real GitHub Projects v2 payloads from a second board (option ids are per-project; T39's fixture set needs two boards with differing `Done` ids) · manual — requires creating a scratch Projects v2 board
- [ ] **T38-fixtures** Capture `rapp-story-ready-for-showcase.json` from the live RAPP board · manual — requires Jira credentials this repo does not hold

---

## Housekeeping

- [ ] Archive accepted rows to `roadmap-history.md` at each phase close

---

## Change Log

| Date       | Change                                                          |
| ---------- | --------------------------------------------------------------- |
| 2026-08-03 | Roadmap created; Phase 1 seeded with the tracker-workflow series (T36–T41) |
| 2026-08-04 | T36 accepted — PR #190 merged (`48bb57e`). One success criterion waived: migration recipe unverified against a live board |
| 2026-08-04 | T37 accepted — PR #193 merged (`deeb795`). All 17 success criteria met, no waivers. 5 QA cycles, 9 findings closed; tests 760 → 840. **T38 and T39 are now unblocked** — they may run in parallel |
| 2026-08-12 | Phase 2 seeded with the document change-log series (T42–T45, issues #201–204). Strictly sequential via `deps:`; T45 `manual`-gated on verifying T44's advisory default. Legend gained `change-log`, `templates`, `review-skills` tags |
| 2026-08-12 | T39 accepted — PR #206 merged (`2c7ff3e`). `gh-stage.js` (1,299 lines), 65 tests, 10 fixtures. All 16 success criteria met, no waivers. 5 QA cycles, 20 findings closed; tests 1050 → 1065. The same defect — writing a status to a board the operator did not name — was caught twice, the second time reopened by a fix's own partial-read tolerance. **T40 is now unblocked** |
| 2026-08-12 | T44 accepted — PR #211 merged (`bd7dcdc`). Fourteen skills now write a Change Log row on mutation; the four `review-*` grade presence + currency per `change-log.enforcement`; `documentation-standards-validator` check (3) defined at last. 21 new protocol tests; tests 1158 → 1175. 1 QA cycle, 1 finding closed. **T45's operator gate is satisfied**: the `advisory` default was verified live rather than by the synthetic check the gate proposes — T44's own document predated T43's template, so its Step 2 review exercised the legacy path and returned **GO with one Important finding** at 9/10. The one defect QA found (an out-of-order instruction list that made an unconditional write read as conditional) passed lint, all 1175 tests, the bundler, both eval suites and doc-link resolution — in a skills library the instruction files are the product, so that class is a real gap in automated coverage |
| 2026-08-12 | T40 accepted — PR #207 merged (`f3d37be`). Five inline board blocks → `gh-stage.js` calls; ~240 lines of duplicated GraphQL deleted. **The GitHub path is now live** — a consumer's ladder drives their board. Three intended behavioural changes (regress guard, case-insensitive Done, honest post-condition). 2 QA cycles, 1 MEDIUM + 3 LOW closed; tests 1065 → 1070, all 5 new guards mutation-tested. Found on the way: **the bundler cannot see `.agents/skills/…/references/X` paths**, so writing a CLI call did not ship it — each site now names `shared/resources/gh-stage.js` and CI gained a bundle-freshness check. Two consumer tests deferred (no scratch board with bespoke columns). **T41 is now unblocked** (deps T38, T40 — both shipped) |
| 2026-08-12 | T41 accepted — PR #208 merged (`0b9d336`). **The tracker-workflow series is complete**: `changes-requested` (per QA fix cycle) and `pr-merged` (from the merging orchestrators, inside `develop-batch`'s per-item loop) now fire; `setup-consumer.sh` scaffolds `tracker-workflow.yaml` and never overwrites; `--init-workflow [--force]` and `--check [--offline]` on both stage CLIs; `develop-bug` reaches parity after signalling nothing for a release. All 15 success criteria met, no waivers. 2 QA cycles, 3 bugs closed; tests 1070 → 1104. Both new moments are off by default and were verified inert against a live board. Found on the way: **the scaffolder inferred "file written" from an exit code this CLI family deliberately returns as 0 on write-nothing skips** — an unauthenticated `gh` would have left consumers with no workflow file while the wizard reported success. Caught by *executing* the code against throwaway consumer repos, not by reading it, and the fix verified by reintroducing the defect to prove the regression test fails. Two residuals named: GitHub's live-probe branch is unreachable from the wizard by construction, and Jira's `--check` board half compares against a local record rather than a live probe. **Phase 2 (T42–T45) is now the frontier** |
| 2026-08-12 | T42 accepted — PR #209 merged (`5b94058`). **Phase 2 has its foundation**: one canonical Change Log spec (`document-change-log.md`) and one engine (`change-log.js`, tracker-agnostic, dependency-free) replacing four incompatible table shapes and a Jira-only implementation. Three long-standing defects die with the extraction — H2-only heading match (which inserted a *second* block above the Epic Goal), an end-scan that swallowed sibling subsections, and the top-of-body insertion fallback — plus a new guard: a marker or heading inside a fenced block **or inline code span** is an example, not a section. No skill behaviour changes; `jira-sync.js` keeps every old export as a wrapper. 14/15 success criteria met; 1 recorded as **not met with reasoning** rather than reworded, because it contradicted the task's own breaking changes. 3 QA cycles, 6 defects closed (2 HIGH, 1 MEDIUM, 3 LOW); tests 1104 → 1144. Found on the way: **every one of the six defects was the same mistake** — a rule stated correctly in the spec, then applied to a subset of the places it governs — and the inline-code half of the fence guard was found by pointing the finished engine at the document that specified it. **T43 is now unblocked** |
| 2026-08-12 | T43 accepted — PR #210 merged (`7dd5e24`). **Every template now emits the canonical section and every `create-*` skill seeds row one**, so a newly created PRD / epic / story / task opens with one Change Log row. Placement follows the sign-off precedent: unnumbered on tasks (the 11-section count re-asserted in the same test block), promoted out of `## Notes & Updates` to its own H2 on epics, and left nested as `### Change Log` on PRDs. All 12 success criteria met, no waivers. 2 QA cycles, 4 findings closed; tests 1144 → 1158. **The documented drift was half the real drift**: two epic-template copies were described as byte-equal and were not — one carried a wholly different frontmatter schema hidden behind an identical line count, so `wc -l` had been mistaken for equality. Phase 2 therefore *replaced* a schema rather than copying three lines, which is a decision; the review made it explicit and added the pre-lock consumer grep that then caught `prd-structure-guide.md` documenting the old shape. **Every defect QA found was in the new instructions, not the templates** — two told an agent to bump a frontmatter `updated:` field its document type does not have, and fixing the first surfaced a second instance the code review had missed; the invariant is now swept for. Two residuals named rather than silently shipped: `review-prd` still writes a five-cell row into the now-four-column brownfield table (**a blocking condition on T44**, as this task's own Risk 4 predicted), and the brownfield *architecture* template carries the same five-column form while its three siblings do not — outside the canonical spec's scope. **T44 is now unblocked** |
| 2026-08-13 | T45 accepted — PR #213 merged (`1aba9f1`). **Phase 2 is complete.** The pipeline now records itself and the sync stops narrating: `develop`, `qa-story`/`qa-task`, `qa-fix` and `finalise` write milestone rows, while the six sync skills narrow from a row per body-hash refresh to two events — issue created, status transition. One marker pair replaces two; the task.42 compatibility wrappers are deleted rather than left orphaned. 20/21 success criteria met; **live-Jira verification recorded as not met** (no credentials, GitHub-tracked repo) rather than quietly ticked. 2 QA cycles, 3 bugs closed; tests 1175 → 1185. The demonstration is the document itself: task.45's own Change Log is eight rows written by the code it ships, with `Version` moving only at creation, review and acceptance. Found on the way, and the reason this row is long: **the engine silently deleted every Change Log row it could not parse** — `upsertChangeLog` regenerated blocks from rows passing a date-first predicate, so a log ordered `\| Version \| Date \|` lost its entire history on first write. This repo's own roadmap template shipped with that ordering. Pre-existing in T42 and not a regression here, but T45 routes five more writers into that path, and "never drops a row it parsed" was the mitigation T45 claimed for its own Critical risk — true, and hollow, since the rows it drops are the ones it fails to parse. Fixed, with the template corrected. Two further T42 engine defects (content loss on the hand-written-heading path; the same-pair collapse skip) were **named and left** rather than folded in mid-QA-cycle. Also caught: a bulk section-replacement regex whose next-heading lookahead matched a heading *inside a fenced sample*, leaving orphaned legacy blocks and unbalanced fences in all six sync skills — the same context-blind class of error recurred once more during finalise and was caught by a guard count |
| 2026-08-13 | **Phases 1 and 2 archived** to `roadmap-history.md` at phase close, per the standing Housekeeping row — the first time that file has been cut, so the header no longer promises a document that did not exist. Ten accepted rows (T36–T45) moved verbatim, keeping their `touches:` tags and acceptance annotations so a `deps:` naming any of them still resolves. The two `-fixtures` rows stay here, deferred and human-gated. This Change Log is deliberately **not** split: it is append-only and it is this document's own history, so the archive references it rather than copying ten long entries out of it. No frontier remains — `select-next.mjs` reports `roadmap-complete` until a new phase is added |
| 2026-08-28 | Phase 3 seeded with the `loop-supervisor` series (T62–T64). Legend gained the `loop-supervisor` tag. T62 is the only mandatory unit; T63/T64 depend on it and hard-conflict with each other on `run-loop.mjs`. Design: `.agents/plans/loop-supervisor.md` |
| 2026-08-29 | **Phase 3 closed and archived.** T62–T64 moved verbatim to `roadmap-history.md`, clearing the live roadmap of completed rows per the standing housekeeping item. Their `deps:` references resolve there, as Phases 1 and 2 already do. No phase now holds an actionable row, so `select-next.mjs` reports `roadmap-complete`; the two Deferred fixture rows (T38/T41) stay invisible to selection and both need credentials this repo does not hold. Filed `bug.2.unbounded-test-concurrency` at the same time — the parallel-pool flake that T62 and T63 were each merged over, which those rows twice said needed its own bug |
| 2026-08-29 | T64 accepted — PR #278 merged. `run-loop.mjs` gains `--dashboard`/`--dashboard-token`: a status frame POSTed at each iteration boundary, ending `active: false`, built from state T62 already writes and adding none. What ships is a **documented payload, not an integration** — the dashboard lives in the consumer repo, so both halves can be built independently and a second consumer is served unchanged. Plus `docs/runbooks/unattended-overnight-runs.md` and the `develop-next` cross-references pointing at the fresh-context alternative to `/loop`. 8/8 success criteria; tests 1834 → 1870. **Phase 3 (T62–T64) is complete.** The reason this row is long: **three QA cycles, twelve findings, on work whose suite was green and whose CI passed from the first push** — nothing a re-run would ever have surfaced. Gate 1 was CONCERNS at **50/100**, and the findings share one shape, the exact failure this task was written to prevent: *coverage claimed where none existed.* The frame published the **whole append-only ledger** while its own README promised per-run totals, so a second run in the same tree would render last night's iterations as current. The token-absence test **could not fail** — proved vacuous by deleting the header it guarded and watching it stay green — while the Risk Assessment rated that leak Low/HIGH and called it "asserted by test". SC2 was proved one level *below* where the criterion states it (at `pushDashboard`, not at the run). And the Risk Assessment named `tests/executable-instructions.test.js` as its mitigation for "runbook documents commands that do not ship" — a test whose `collectDocs()` **had never opened a runbook or a skill README**, so the two documents this task created were unscanned by the gate cited to protect them. Widened and mutation-proved on both. Cycle 2 then re-verified every cycle-1 fix **by mutation rather than by reading it**, and found one more of the same family: the child-environment token strip was correct in code and **held by nothing** — it sat inline in `main()`, unreachable from any test, so deleting it left the suite fully green. That is cycle 1's vacuous test wearing different clothes, so it gated rather than being waved through; `childEnvFor()` is now exported with tests in **both** directions, because a single "the token is gone" assertion would have passed against a function returning `{}` and breaking every spawned iteration. 10 invariants probed, 10 proved. **Two further defects were introduced by the fixes themselves and caught before CI**: the double-SIGINT fix opened a third-SIGINT re-entrancy race (found by the adversarial pass over the fixes, not by QA), and the QA report linked `.summaries/`, which `.gitignore` excludes — a link that resolves locally and 404s in CI, found by checking the **tracked** tree in a detached worktree after CI had already caught one dead anchor earlier in the run. Unlike T62 and T63, this merged on a **fully green local suite and green CI on the final head**; the parallel-pool flake those two rows describe did not recur, and still wants its own bug |
| 2026-08-29 | T63 accepted — PR #277 merged (`cbc909b`). `run-loop.mjs` gains `status` and `watch` — pure readers over the artifacts T62 writes, safe from a second terminal, over SSH, concurrently and mid-iteration — plus `--notify`/`--webhook` firing **once** on terminal stop. The renderer is a pure function both views wrap (`references/render.js`), with `nowMs` and `isAlive` **injected**, which is the choice that makes the dead-pid state testable without arranging a real dead process. 9/9 success criteria met, plus a tenth QA added. 2 QA cycles, 1 MEDIUM closed; tests 1794 → 1834 (150 for the skill), **7 invariants mutation-proved**. Found on the way, and the reason this row is long: **the task document described the ledger inaccurately, and both the fix and the review of the fix repeated the same mistake in new forms.** Review caught `numTurns` (the ledger writes `turns` — `num_turns` is the *envelope's* name) and an undocumented second row shape (`spawned: false` probe-stop rows carry seven fewer fields and are the **normal last line of a healthy run**), before either reached the renderer. QA cycle 1 then found the real defect: `readCurrent` collapsed *absent* and *unparseable* into `null`, so a torn heartbeat rendered as **"no run in flight"** — the reassuring answer at the moment it was least deserved, and the same failure the task's own Decisions table exists to prevent, worse in one respect because a crashed report makes someone look while "no run in flight" ends the investigation. Fixed at both ends (reader distinguishes the two from one `readFileSync` and the error code; writer is now atomic via temp+rename) — and the reader fix was **kept despite** the writer fix, because a view should not depend on the writer being careful in order to avoid stating a falsehood. QA cycle 2 then found that the *fix* had introduced the same shape pointed the other way: the new sentinel was duck-typed across a module boundary, so a valid heartbeat carrying that key was misreported as unreadable. Now compared by identity. **Merged over a red local `npm test` on operator decision, as T62 was** — but this time the flake was pinned down rather than tolerated: a controlled experiment (clean `develop` plus two *filler* test files containing 30 trivial assertions and no task code) fails **five** tests including the same `§8b`, while this branch fails one and CI passes both. The suite has a pre-existing parallel-pool fragility — five spawn-heavy tests carrying 20–30s wall-clock budgets for work that completes in under a second — and it needs its own bug; the likely fix is bounding `node --test` concurrency, not raising five timeouts. **T64 is now unblocked** (it hard-conflicts with T63 on `run-loop.mjs`, so the conflict is now resolved rather than pending) |
| 2026-08-28 | T62 accepted — PR #276 merged (`c113362`). `skills/loop-supervisor` ships the supervisor process and its artifacts: `run-loop.mjs`, the pure `classify.js`, three adapters, pinned `--settings`, and 110 unit tests. 7 of 8 success criteria met; **SC5 (one real `/develop-next` iteration) deferred to a post-merge operator step** — it cannot run inside the pipeline implementing it, which holds both `develop-next.state.json` and `develop-pipeline.lock` and would merge an unrelated roadmap item as a side effect of testing. 2 QA cycles, 1 finding closed: config silently overrode an explicit CLI flag equal to the default, which would have pointed the progress oracle at the wrong ref and made every healthy iteration read as `idle`. The DoD then caught a gap both QA cycles missed — no `CHANGELOG.md` entry. Merged over a red local `npm test` on operator decision: the one failure is a pre-existing 30s timeout in `jira-interception.test.mjs` that reproduces on clean `develop`, passes 48/48 in isolation, and passes in CI on this head. **T63 and T64 are now unblocked** — they hard-conflict on `run-loop.mjs`, so `--batch` will take only one at a time |
| 2026-08-29 | B2 closed — PR #279 merged (`6bfc65a`). `npm test` now bounds `node --test` on all five invocations, and a new shared `spawn-budget.mjs` (60 s, env-tunable, `{PREFIX}_` → `TEST_` → default) replaces **all eleven** bare timeout literals in `jira-interception.test.mjs` and `access-config-parity.test.mjs`. **Measurement inverted the fix the bug asked for.** Bounding is free — 36–39 s across unbounded/4/8/unbounded — but on a quiet box it buys nothing: the worst single test is ~2.6–2.8 s at *every* concurrency including unbounded, and the spread between two **unbounded** runs is as wide as bounded-vs-unbounded, i.e. the suite never self-oversubscribes enough to threaten a 20 s timeout. What collapses the margin is external load the suite does not own — 16 competing spawn loops put the worst test at 16.2 s unbounded and **17.1 s bounded** — so option 1 cannot help there. The detail that settled it: the file that *absorbed* that 16.2 s spike was `access-config-parity.test.mjs`, the one file already carrying a 60 s budget, while `jira-interception.test.mjs` (the historical victim) peaked at 4.3 s. **Shipping the bound alone would have closed this bug without changing the failure probability** — the worst outcome available, since the flake returns against a bug marked fixed. Both halves therefore shipped, and the parity suite's remedy was extracted rather than copied: it had applied its own fix to 1 of its 12 spawn sites. The reason this row is long: **four of five verify cycles failed, and every failure was in the guard rather than the fix** — fitting for a bug about a defective gate, and three of them the same shape, *a guard passing on the exact regression it was named after*. Cycle 1: prettier, which CI runs **before** the suite. Cycle 2: a comment stripper that deleted 53 lines of a real suite (a `/*` inside a `//` opened a phantom block), a detector blind to `node --flag --test`, `SPAWN_RETRIES=0` silently becoming 2, and `??` letting an **empty** `{PREFIX}_` var mask the global knob — the normal shape of an unset CI input, so it broke in precisely the tune-the-CI-box case the module exists for. Cycle 3: the *fixed* stripper deleted a line after every regex literal ending in `\/` (11 real sites) and preserved comments after `/['"]/`; two guards were also proved to be measuring nothing — `runnerScripts()` was never called by its own tests, so reverting it changed nothing observable. Cycle 4: the scanner still did not lex template substitutions, so a nested template containing a URL or glob deleted to EOF — a construct already present in the corpus. The lesson recorded in the bug: **a regex cannot lex JavaScript**, and *the fixture, not the implementation, decides whether a guard is worth anything* — each broken version had a passing test whose fixture omitted the one construct that broke it. Settled by a whole-corpus check (strip all 74 scanned files, parse the output, expect zero failures — an earlier revision produced five) and 10 mutations each reddening exactly one guard. Also found: a mutation using `sed '0,/re/s//repl/'` silently did nothing on BSD sed, and the resulting green suite read as an unheld test — **verify the mutation, not just the test**. Tests 1877 → 1893. Four residual limitations recorded rather than buried, incl. `TEST_CONCURRENCY=0` being silently ignored by node, which no static guard can see. Adjacent and explicitly out of scope: `package.json`'s test globs are hand-maintained, 11 of 21 one-skill-one-line, so a new `skills/*/tests/` dir is silently uncovered — nothing orphaned today |
| 2026-08-29 | **Phase 4 retired**, not merely emptied — its two rows (B2 accepted, T65 in flight) moved to `roadmap-history.md`, and the "How to use" preamble rewritten to say why no standing maintenance phase should replace it. T65 makes the phase's own stated purpose obsolete: selection now falls through to the bug and task registries when no phase holds an actionable row, so filed work reaches the loop without the hand transcription this phase *was*. The archival is part of T65's delivery rather than a follow-up, because roadmap precedence is absolute — while Phase 4 held an actionable row the new fallback could never fire, and the feature would have merged and changed nothing observable. T65's row is archived **unticked** (it was in flight); its acceptance is recorded here on merge |

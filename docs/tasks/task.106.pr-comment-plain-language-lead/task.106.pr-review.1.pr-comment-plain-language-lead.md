# PR Review Report: PR #381 — feat(task.106): the plain-language lead reaches the pull request

**Reviewed:** 2026-09-10
**PR:** [#381](https://github.com/Gamaroff/agent-skills/pull/381) — `feature/task.106.pr-comment-plain-language-lead` → `develop` (OPEN)
**Work item:** [`task.106.pr-comment-plain-language-lead.md`](./task.106.pr-comment-plain-language-lead.md) — resolved via `branch stem`
**Tracker:** [#380](https://github.com/Gamaroff/agent-skills/issues/380) — open
**Verdict:** ✅ **APPROVE** (both findings fixed under this review; see Disposition)

**Scope reviewed:** 2,732 lines across 22 files. Excluded `*/references/*` — 46 files generated by
`npm run bundle` from `shared/resources/`, byte-identical to their sources.

---

## Artifact Trail

| Artifact | Status | Detail |
|---|---|---|
| Implementation report | ✅ | `task.106.implementation.1.…-initial-run.md` |
| Review report | ✅ | `task.106.review.1.…md` (READY TO IMPLEMENT, 9/10) |
| QA reports | 1 | `task.106.qa.1.…md` |
| Gate | **PASS** | `task.106.gate.3.…yml` (90). Cycle 1 CONCERNS 80 → cycle 2 PASS 95 → cycle 3 PASS 90 |
| DoD | ❌ | `/finalise` has not run — correct at this step, not a gap |
| Sprint review | ❌ | as above |
| Open bugs | 0 | — |
| Handover | ✅ | none outstanding (`access.tracker: full`) |

---

## Acceptance Criteria Traceability

The conformance lens traced 11 of 12 to real diff evidence independently; the twelfth was undermined
by PC-1 and is repaired.

| Criterion | Evidence | Status |
|---|---|---|
| Eleven templates led, both arms | 11 invocations across 7 files | ✅ |
| `buildSummaryBody()` leads; `--summary-file` not double-led | `pr-inline-comment.js:289`; §10 tests | ✅ |
| Inline findings carry no lead, asserted | two tests — shape **and** source construction | ✅ |
| Sites 2 and 10 idempotent | marker at byte 0; `startswith` searches intact | ✅ |
| Board warnings state consequence; site 4 keeps its record id | `finalise:1425/1436/1449` | ✅ |
| **Site 6 (dod-gaps) posts the gaps** | **was PC-1 — awk over-captured; now bounded** | ✅ repaired |
| No new network calls | every lead rides an existing call | ✅ |
| One insertion point per site | 11/11, verified by reassembling continued commands | ✅ |
| No new vocabulary | all templates from `stakeholder-summary.js` | ✅ |
| PR stages absent from `COMMENT_STAGES` | engine rejects all three by name, with a control | ✅ |
| No `references/` hand-edited | bundle idempotent, 0 changed | ✅ |
| Contract + standard document the exclusion | `pr-inline-comment-contract.md`, `stakeholder-summary.md` | ✅ |

---

## Conformance Findings

**[PC-1] trail · high · confidence: high — `skills/finalise/SKILL.md:1623`**
The cycle-2 fix for T106-001 bound `GAP_REPORT_BODY` with an unbounded `awk`, capturing to
end-of-file. Every task and story document carries `## Progress Tracking` checkboxes after the gaps
section, so this fires on essentially **every** real gaps comment — not occasionally. Measured:
GAP_COUNT=5 on a two-gap fixture, four unrelated sections pasted into the body. The reviewer's
verdict on the trail is the material point: *gate 2's PASS and "0 open issues" for T106-001 was not
fully earned* — the fix closed the symptom, not the behaviour.
→ **Fixed** under this review: bounded to the section. Proved behaviourally (fixed 2/0, mutant 5/4).

**[PC-2] consistency · medium · confidence: high — §7 Files Summary**
§7 listed `review-pr.test.js`, `review-code.test.js` and `qa-execute-snippets.test.mjs` as modified;
the diff touches none of them, and omitted `transition-protocol-parity.test.mjs`, which it does.
The reviewer noted the author had already corrected one sibling row the same way — so the pattern was
known and not applied.
→ **Fixed**: §7 now records what changed, and why each unchanged file did not need changing.

---

## Code Review Findings

**[CR-1] bug · high · confidence: high — `skills/finalise/SKILL.md:1616`**
Same defect as PC-1, found independently by the other lens. Reproduced against a fixture.
→ **Fixed.**

The code lens explicitly **cleared** what it was asked to press on: all 11 sites bind their inputs in
scope; `stakeholder-summary-cli.js` has no silent-empty/exit-0 path (every template is non-empty with
`{}`, unknown stage is a hard exit 2); and the new `PR_SITES` collector, Guard C's resolvability
assertion and the §10 double-lead test all exercise real call sites with no vacuous pass.

---

## Findings the review deliberately did not raise

The conformance lens ruled on two questions this review was asked to press hardest, and ruled
**for** the change:

- **`PR_COMMENT_STAGES` is not the enumeration anti-pattern.** `COMMENT_STAGES` is enforced at
  runtime as an argument validator; `PR_COMMENT_STAGES` has no runtime enforcer and exists only to
  make the partition checkable. That is the anti-patterns doc's prescribed population-check cure
  applied correctly. Held disjoint-and-exhaustive by four dedicated tests plus the third-CLI fix to
  `transition-protocol-parity.test.mjs`.
- **`stakeholder-summary-cli.js` is not scope creep.** A deferred decision in the plan file, taken
  up and recorded in §7 with rationale.

---

## Machine-Readable Findings

```yaml
findings:
  - id: PC-1
    category: trail
    severity: high
    confidence: high
    ref: "skills/finalise/SKILL.md:1623"
    finding: "The cycle-2 fix bound GAP_REPORT_BODY with an unbounded awk, capturing to end-of-file and counting unrelated checkboxes as gaps."
    suggested_action: "Bound the match to the section; treat T106-001 as closed only then."
  - id: PC-2
    category: consistency
    severity: medium
    confidence: high
    ref: "task.106.pr-comment-plain-language-lead.md §7"
    finding: "Three test files listed as modified are untouched by the diff; one that is modified is omitted."
    suggested_action: "Record what actually changed, and why each unchanged file did not need changing."
  - id: CR-1
    category: bug
    severity: high
    confidence: high
    ref: "skills/finalise/SKILL.md:1616"
    finding: "The GAP_REPORT_BODY awk has no stop condition and over-captures to end-of-file."
    suggested_action: "Add the stop condition at the next '## ' heading."
truncated_count: 0
```

---

## Disposition

All three findings were **fixed under this review**, so the verdict computed on the *remaining*
findings is APPROVE. Stated explicitly because the deterministic table applied to the findings **as
found** gives 🚨 REQUEST CHANGES — two high/high — and a reader should be able to see both readings
rather than only the flattering one.

**One coverage gap stands, and is not closed by any fix here.** No harness executes `finalise`'s
shell blocks: Step 4b refuses them as `mutating`, and `node` and `awk` are fail-closed in the snippet
engine's allow-list. PC-1/CR-1 is proved by reproduction, not by a committed regression test — so
this defect class can recur with no suite going red. Recorded in gate 3 as `coverage_gap` with an
owner.

## Recommended Actions

1. **Done** — bound the awk to its section, and re-derive the count from the bounded body.
2. **Done** — reconcile §7 with the diff.
3. **Future** — extend the snippet engine to execute repo-local read-only commands, so this block
   acquires a guard. `shared/resources/qa-execute-snippets.mjs`.
4. **Future** — a check that a shell variable a documented block interpolates is bound in that block.
   Guard C covers slot *names*; the variable half is what shipped T106-001.

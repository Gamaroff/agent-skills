schema: 1
task: 'task.136.shell-fn-probe-entry-form'
task_title: 'A shell-fn: entry form for the security probe, and a fake-gh affordance'
gate: CONCERNS
status_reason: 'Re-review cycle 3 (scoped to files changed since gate 2, at a99f881f). TASK-136-BUG-2 verified FIXED by execution and closed; cycle-2 advisories CR-2/CR-4/CR-5 verified closed. ci:fast 3887/3888 (0 fail); security measured (41 executed; the form engages on the live boundary). No HIGH, no MEDIUM in the queue: the diff review returned three medium-confidence findings — each verified true by QA but rated medium confidence by the reviewer, so advisory by the mapping rule — that are limits of the new mechanism (a library installing its own EXIT trap displaces the source guard; the needs-fake-gh decline covers shell-fn: only and its gh detector misses gh; / gh> / $GH / transitive source). Reliability CONCERNS on those limits; the queue is empty, so this gate reaches 5c.'
reviewer: 'QA Engineer'
updated: '2026-09-21T20:48:59Z'

top_issues: []

waiver:
  active: false

quality_score: 90

evidence:
  tests_reviewed: 3888
  phases_verified: 4/4
  trace:
    phases_covered: [1, 2, 3, 4]
    phases_with_issues: []

nfr_validation:
  security:
    status: PASS
    evidence: measured
    probes_executed: 41
    notes: 'Record task.136.qa.3.security.run.json (totals.executed 41) on a99f881f: gh-labels-shell-fn engages (bash+zsh, 20 executed, 0 reproduced, 0 escapes); containsShellFnEntry present-but-inert only on shellfn.symlink-escape (pre-existing resolveEntry limit, task.128 gate 5, future). BUG-2 verified by execution (top-level exit → entry-not-probeable executed 0; set -e + return 97 → exit 99 ≠ 0). needs-fake-gh verified (bare gh-labels run → unverifiable/needs-fake-gh, executed 0). QA cycle-3 mutants: trap disarm removed → green row red (covered); EXIT trap removed → BUG-2 row red (covered); GH_COMMAND_WORD never matches → CR-2 row red (covered).'
  performance:
    status: PASS
    notes: 'Unchanged.'
  reliability:
    status: CONCERNS
    notes: 'Three verified limits of the new mechanism, all advisory (medium confidence as returned): c3-CR-1 a library that installs its own top-level trap … EXIT before a || exit 1 guard displaces the source guard and is scored absent with a full count (bash+zsh: rc 1) — shadow exit during the source (exit() { builtin exit 97; }; unset -f exit after); c3-CR-2 the needs-fake-gh decline is gated on kind shell-fn only, so a shell: script that names gh and was given no --fake-gh still runs the host gh and is scored (pre-existing for the shell: form since task.128; the flag header says it covers both shell forms) — apply to isShellForm; c3-CR-3 GH_COMMAND_WORD reads only the entry file and requires trailing whitespace, so gh; gh>/dev/null (gh) "$GH" api and a transitive source of gh-labels.sh are run bare — widen the terminator class and follow one level of source.'
  maintainability:
    status: PASS
    notes: 'Cycle-2 CR-4 (collides half) and CR-5 (fixture header) verified closed.'

recommendations:
  immediate: []
  future:
    - action: 'c3-CR-1: shadow exit during the source so a library-installed EXIT trap cannot pre-empt the 97 sentinel; row with a library that sets its own trap then exits'
      refs: ['shared/resources/security-probe.mjs']
    - action: 'c3-CR-2: apply the needs-fake-gh decline to every shell form (isShellForm && fakeGhDir === null) with the shell: counterpart row'
      refs: ['shared/resources/security-probe.mjs']
    - action: 'c3-CR-3: widen GH_COMMAND_WORD terminators to [\s;|&)>]|$ and follow one level of top-level source/. so indirect gh reach is declined too'
      refs: ['shared/resources/security-probe.mjs']
    - action: 'shellfn.symlink-escape: pre-existing resolveEntry limit (no realpath before containment), carried from task.128 gate 5'
      refs: ['shared/resources/security-probe.mjs']

bug_resolution:
  bugs_fixed: 2
  bugs_remaining: 0
  fix_date: '2026-09-21'
  total_iterations: 2
  verification_method: 'Execution against a99f881f + 3 QA mutants per cycle'

deployment_readiness:
  staging: APPROVED
  production: CONDITIONAL
  conditions: ['Follow-up task for the three cycle-3 advisories (shell-fn sentinel/fake-gh coverage limits)']

schema: 1
task: "task.71.selection-floor-matches-dispatcher"
task_title: "Make the selection floor equal what the dispatching pipeline accepts"
gate: CONCERNS
status_reason: "Implementation, tests and mutation proofs are correct and complete — all 11 success criteria met, 1999 tests green. One MEDIUM maintainability defect: twelve comment lines in the test file carry literal \\u2286 / \\u2014 escape sequences instead of the characters they denote, including the H1 section header."
reviewer: "QA Engineer"
updated: "2026-08-31T19:05:00Z"

top_issues:
  - id: TASK-71-QA1-01
    issue: "Literal escape sequences in test-file comments render as raw text"
    severity: medium
    category: maintainability
    finding: "evals/develop-next/unit/select-next.test.mjs lines 1818, 1823, 1826, 1828, 1829, 1831, 1833, 1901, 1913, 1916, 1939, 1947 contain the literal characters backslash-u-2-2-8-6 (and -2014, -2192) inside // comments, where JS performs no escape processing. The H1 section header — the first explanation a future author reads — is among them. Caused by \\u escapes written in a non-raw Python heredoc during authoring."
    suggested_action: "Replace the literal sequences with the real characters in the twelve comment lines; optionally normalise the three template-literal occurrences for consistency. Text-only, no assertion depends on them."
    suggested_owner: dev
    bug_ref: "task.71.bug.1.literal-unicode-escapes-in-comments.md"
    status: open

waiver:
  active: false

quality_score: 90

evidence:
  tests_reviewed: 1999
  tests_passing: 1998
  tests_failing: 0
  tests_skipped: 1
  tests_added: 2
  tests_rewritten: 3
  phases_verified: "4/4"
  mutation_proofs_executed: 3
  mutation_proofs_passed: 3
  trace:
    phases_covered: [1, 2, 3, 4]
    phases_with_issues: []
    success_criteria_covered: 10
    success_criteria_uncovered: 0

nfr_validation:
  security:
    status: PASS
    notes: "No credential, network, filesystem-write or user-input surface touched. The change is one Set literal plus comments and tests. No new dependencies."
  performance:
    status: PASS
    notes: "Set membership on a 4-element set instead of a 2-element set. Selection short-circuits at the first eligible row, so a wider floor can only reduce the number of documents read, never increase it."
  reliability:
    status: PASS
    notes: "Excluded statuses are still exactly the three develop-task HALTs on, so the loop-cannot-self-recover failure task.65 identified remains prevented. Rollback is one line, documented in the task's §11. Roadmap precedence is unchanged and asserted in its strong form (loader never called)."
  maintainability:
    status: CONCERNS
    notes: "Rationale prose is thorough, rewritten rather than deleted at every site, and the reversed decision is explicitly narrated. But twelve comment lines in the primary test file render as literal escape sequences, including the H1 section header — a documentation defect in a change whose stated purpose is that the prose states the rule correctly."

code_review:
  bugs_found: 0
  cleanups_found: 2
  blocking_enabled: true
  promoted_to_top_issues: 0
  notes: "code_review_blocking=true was in effect. Zero category:bug findings, so nothing was promoted. The two findings are cleanups: the literal-escape defect (raised separately as a QA MEDIUM, since it is a documentation defect rather than a diff-correctness bug) and a LOW preference for assert.deepStrictEqual over assert.deepEqual."

recommendations:
  immediate:
    - action: "Replace the literal escape sequences with real characters in the twelve comment lines"
      refs: ["evals/develop-next/unit/select-next.test.mjs"]
  future:
    - action: "Prefer assert.deepStrictEqual over assert.deepEqual in 16/H1 — both sides are string arrays so behaviour is identical today, but strict is the documented recommendation and removes a loose-equality footgun if the shape ever changes"
      refs: ["evals/develop-next/unit/select-next.test.mjs:1923"]
    - action: "The bug-axis divergence (in-progress, ready-for-qa) remains deliberately open — file a task when closing it is wanted"
      refs: ["skills/develop-next/scripts/select-next.mjs"]

deployment_readiness:
  staging: CONDITIONAL
  production: CONDITIONAL
  conditions:
    - "Fix TASK-71-QA1-01 (comment escape sequences) before merge"

schema: 1
task: 'task.149.qa-evidence-integrity'
task_title: 'QA evidence integrity: qa-task/qa-story claims that no check reads back'
gate: CONCERNS
status_reason: 'BUG-6 and BUG-7 closed by consolidating the read-back into qa-read-back.js; all three boundaries engage (48 probes, 0 reproduced). The new script still reaches three could-not-look states without exit 2 and misses a gate qa-cycle.sh counted (MEDIUM each).'
reviewer: 'QA Engineer'
updated: '2026-09-26T11:35:00Z'

top_issues:
  - id: TASK-149-BUG-8
    severity: medium
    file: 'shared/resources/qa-read-back.js'
    finding: 'Three could-not-look states do not exit 2: a directory --doc is git-added whole before the read fails; a checkDocument disk fallback (tracked false) reports clean without reading the index; uncaught throws exit 1 as if a HALT (review CR-1, CR-3, CR-4).'
    suggested_action: 'Refuse a non-regular --doc with exit 2 before any git add; exit 2 when links.tracked is false; wrap the body and map unexpected errors to exit 2; test each.'
    suggested_owner: dev
    bug_ref: 'task.149.bug.8.read-back-could-not-look-not-exit-2.md'
  - id: TASK-149-BUG-9
    severity: medium
    file: 'shared/resources/qa-read-back.js'
    finding: 'With the cycle known, a gate the lookup regex cannot match (x.gate.4.yml, x.gate.04.name.yml — accepted by qa-cycle.sh) is neither staged nor halted on; there is no !gate branch (review CR-2).'
    suggested_action: 'Halt when the cycle gate is not found and match names with the qa-cycle.sh grammar; test both spellings.'
    suggested_owner: dev
    bug_ref: 'task.149.bug.9.read-back-gate-grammar.md'

waiver:
  active: false

quality_score: 80

evidence:
  tests_reviewed: 4216
  phases_verified: 5/5
  trace:
    phases_covered: [1, 2, 3, 4, 5]
    phases_with_issues: [4]

nfr_validation:
  security:
    status: PASS
    evidence: measured
    probes_executed: 48
    notes: '--copy-as DEST containment engages 24/24 (SRC "/" included); change-log --check-updated engages 14/14; qa-read-back verdict engages 10/10; TMPDIR=/tmp 166 pass.'
  performance:
    status: PASS
    notes: 'qa-read-back suite 14 cases ~4 s; wiring test 14 cases concurrent.'
  reliability:
    status: CONCERNS
    notes: 'TASK-149-BUG-8, TASK-149-BUG-9.'
  maintainability:
    status: PASS
    notes: 'Advisory cleanups: CR-5 double report of a failed stage; CR-6 DEST check still a string prefix beside isWithin.'

bug_resolution:
  bugs_closed: ['TASK-149-BUG-6', 'TASK-149-BUG-7']
  bugs_new: 2

recommendations:
  immediate:
    - action: 'Map every could-not-look state to exit 2 (TASK-149-BUG-8); find or halt on the cycle gate (TASK-149-BUG-9).'
      refs: ['shared/resources/qa-read-back.js']
  future:
    - action: 'CR-6: use isWithin(tmp, target) for the DEST check too, so the file has one containment test.'
      refs: ['shared/resources/qa-execute-snippets.mjs']

deployment_readiness:
  staging: CONDITIONAL
  production: CONDITIONAL
  conditions: ['TASK-149-BUG-8 and TASK-149-BUG-9 fixed']

schema: 1
task: "task.75.quality-gate-matches-ci"
task_title: "Make the pipeline quality gate run what CI runs"
gate: PASS
status_reason: "All five findings across three cycles are closed and verified. 4/4 phases implemented, every success criterion holds mechanically, full `npm run ci` green, and the contract test is mutation-proved 10x including the proof that the cycle-2 scoping fix removes a real false failure rather than restating existing behaviour."
reviewer: "QA Engineer"
updated: "2026-09-01T14:15:00Z"

top_issues:
  - id: "TASK-75-001"
    severity: medium
    file: "shared/resources/develop-pipeline-step-5-6-qa-loop.md"
    finding: "Fast-gate block numbered 0a. but placed before step 0."
    suggested_action: "Reorder to 0 -> 0a -> 1."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-01"

  - id: "TASK-75-002"
    severity: medium
    file: "evals/shared/tests/ci-gate-parity.test.mjs"
    finding: "Parity scan documented as the `test` job but read every run: line in the file; correct only while test.yml holds one job."
    suggested_action: "Scope the scan to the named job; assert the job resolves."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-01"

  - id: "TASK-75-003"
    severity: low
    file: "shared/resources/develop-pipeline-step-5-6-qa-loop.md"
    finding: "Step 0a claimed MAX_ITER bounded its inner retry; MAX_ITER counts cycles and never governed it."
    suggested_action: "State an explicit retry budget."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-01"

waiver:
  active: false

quality_score: 100

evidence:
  tests_reviewed: 2094
  phases_verified: "4/4"
  trace:
    phases_covered: [1, 2, 3, 4]
    phases_with_issues: []

nfr_validation:
  security:
    status: PASS
    notes: "No security surface touched across all three cycles — no new dependencies, no credential handling, no network calls. Staged diffs scanned for secrets and debug logging each cycle; clean."
  performance:
    status: PASS
    notes: "The merge gate is slower by design and the trade is argued and recorded. Tiering is enforced rather than intended: the parity test asserts `ci:fast` is a strict subset of `ci`, verified at runtime as format:check + test with zero occurrences of the slow tier."
  reliability:
    status: PASS
    notes: "CONCERNS at cycle 2 (TASK-75-003) resolved: the inner gate retry now carries a real 2-attempt budget instead of naming a bound that never governed it. Rollback remains a one-line default revert leaving CI untouched."
  maintainability:
    status: PASS
    notes: "CONCERNS at cycle 2 (TASK-75-002) resolved: the parity scan now reads what it documents. Six independently-drifting restatements of one default are held by a contract test, and the fast gate is a config key rather than a literal so consumer repos are not told to run a script they lack."

recommendations:
  immediate: []
  future:
    - action: "OUT OF SCOPE — `qa-execute-snippets.mjs` exits 0 with zero output when invoked through a symlinked path, which is the path its own documentation prescribes (`.agents/skills/...`). The QA step built to catch prose that is never executed silently never executes. `select-next.mjs:1486` already carries the exact fix and a comment describing this defect. Warrants a general bug report."
      refs: ["shared/resources/qa-execute-snippets.mjs"]
    - action: "OUT OF SCOPE — `access-config-parity` failed 2 of 3 full runs this session on `spawnSync bash ETIMEDOUT` under load (32/32 in isolation, twice). This task promotes that suite to a mandatory pre-merge gate, which is what makes its flakiness matter: a gate that fails two-in-three teaches people to re-run until green. Fix is a realistic spawnSync timeout or dropping the subprocess."
      refs: ["shared/resources/tests/access-config-parity.test.mjs"]
    - action: "OUT OF SCOPE — develop-bug's per-cycle fix loop has no fast gate; its develop loop does, via the shared step-3 document. A bug fix cycle can still commit an unformatted tree where a task fix cycle now cannot."
      refs: ["shared/resources/develop-bug-step-5-6-verify-loop.md"]

deployment_readiness:
  staging: APPROVED
  production: APPROVED
  conditions: []

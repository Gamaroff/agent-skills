schema: 1
task: 'task.128.shell-boundary-probe-and-finalise-recheck'
task_title: 'A refusing shell script is a boundary the probe engine cannot reach, and finalise can only accept or halt'
gate: FAIL
status_reason: 'Cycle-1 fixes BUG-1..4 verified FIXED by execution (symlinked CLI runs; missing script declines; NUL is bad-entry; a boolean no longer proves). The refute pass and the safety re-probe found one HIGH — a side effect a script writes to its cwd is invisible to absent and the sentinel (the no-cd script shape) — and three MEDIUM: Step 8a licenses on forecast commits/touched never checked against git; a target 126/127 exit is declined instead of compared (cycle-1 fix 2 converted a reproduction into a decline); an empty expected scores the pre-fix script engages. Two LOW bugs (red marker not tied to the named test; record lacks shells) and three cleanups.'
reviewer: 'QA Engineer'
updated: '2026-09-20T18:57:33Z'

top_issues:
  - id: 'TASK-128-BUG-5'
    severity: high
    file: 'shared/resources/security-probe.mjs'
    finding: 'runShellCase spawns with cwd: workDir but checks expected.absent against fixtureDir, and snapshotTree skips workDir — a script that re-parses a name without cd "$1" (the qa-cycle.sh shape) runs $(touch PWNED) where neither absent nor the sentinel looks; the case scores rejected (refute review CR-1, reproduced with a no-cd fixture: outcome rejected, escapes 0).'
    suggested_action: 'Spawn with cwd: fixtureDir (or check absent against both), and add a no-cd eval fixture to the tests.'
    suggested_owner: dev
    status: open
    bug_ref: 'task.128.bug.5.side-effect-in-child-cwd-invisible-to-absent-and-sentinel.md'
  - id: 'TASK-128-BUG-6'
    severity: medium
    file: 'shared/resources/finalise-fix-and-recheck.mjs'
    finding: 'Step 8a licenses the commit on "commits": 1 (hard-coded before any commit exists) and touched ("every path the fix WILL change") — forecasts the evaluator never compares with git rev-list --count / git diff --name-only, the self-report class BUG-4 closed for mutation-proved (refute review CR-2).'
    suggested_action: 'Add --git-base <ref>: derive commits and touched from git, fail closed on disagreement with the record; Step 8a runs it post-commit as the licence to push.'
    suggested_owner: dev
    status: open
    bug_ref: 'task.128.bug.6.fix-and-recheck-licence-issued-on-forecast-not-record.md'
  - id: 'TASK-128-BUG-7'
    severity: medium
    file: 'shared/resources/security-probe.mjs'
    finding: 'The 126/127 branch is reached by bash failing to open the file AND by the target exiting 126/127 itself, and reports one value (errored); the stat check already removes the first, so in practice a target-produced 127 (a hostile name run as a command under set -e) is declined instead of accepted — cycle-1 fix 2 converted a reproduction into a decline (refute review CR-3; verified: bash "$1" ignores the shebang, launch failures print bash: <path>: No such file|Is a directory|Permission denied).'
    suggested_action: 'Key the errored branch on bash launch-failure stderr for entryPath, not on the exit code alone; compare a target-produced 126/127 like any other exit.'
    suggested_owner: dev
    status: open
    bug_ref: 'task.128.bug.7.target-exit-126-127-declined-instead-of-compared.md'
  - id: 'TASK-128-BUG-8'
    severity: medium
    file: 'shared/resources/security-probe.mjs'
    finding: 'compareExpected compares only the keys present, so a --cases-file case with expected: {} (or absent-only) matches every run and the PRE-FIX qa-cycle.sh scores engages with executed 4 — a probe that compared nothing reads as a control that holds (safety re-probe, reproduced).'
    suggested_action: 'Decline a case whose expected carries none of stdout / exit / stderr; test with empty and absent-only expected against the pre-fix fixture.'
    suggested_owner: dev
    status: open
    bug_ref: 'task.128.bug.8.empty-expected-scores-a-vacuous-engages.md'
  - id: 'CR-4'
    severity: low
    file: 'shared/resources/finalise-fix-and-recheck.mjs'
    finding: 'mutation-proved tests text.includes(m.test) and RED_MARKER independently over the whole log, so a whole-suite log where the named test is green and an unrelated test is red satisfies both.'
    suggested_action: 'Tie the red marker to the named test: a red line within the named test/file block, not anywhere in the file.'
    suggested_owner: dev
    status: open
  - id: 'CR-5'
    severity: low
    file: 'shared/resources/security-probe.mjs'
    finding: 'The header and result comment say the record says which shells ran, but toRecordEntry never copies result.shells — the record and the emitted block carry executed = cases × shells with no shells field.'
    suggested_action: 'Add shells to toRecordEntry and the emitted block; extend the record-shape test for a shell-form run.'
    suggested_owner: dev
    status: open

waiver:
  active: false

quality_score: 50

evidence:
  tests_reviewed: 3619
  phases_verified: 3/3
  trace:
    phases_covered: [1, 2, 3]
    phases_with_issues: [1, 3]

nfr_validation:
  security:
    status: CONCERNS
    evidence: measured
    probes_executed: 39
    notes: 'Re-run on the fixed tree, recorded to task.128.qa.2.security.run.json (totals.executed 39): path corpus against resolveEntry via shell: — null-byte now rejected (BUG-3 FIXED), symlink-escape still accepted (documented pre-existing limit, future); filename corpus against qa-cycle.sh — engages 28/28. New: BUG-5 (a side effect in the child cwd is invisible) and BUG-8 (an empty expected is a vacuous pass) are both ways a probe that saw nothing reports a control that holds.'
  performance:
    status: PASS
    notes: 'Unchanged: 1.35 s shell entry; JS path unchanged.'
  reliability:
    status: CONCERNS
    notes: 'BUG-1..4 fixed and re-verified. BUG-7: a target 126/127 exit is declined rather than compared. BUG-6: the fix-and-recheck licence is issued on forecast inputs.'
  maintainability:
    status: PASS
    notes: 'Step 4b cycle 2: 2 runnable blocks executed under bash and zsh, no disagreement; TMPDIR=/tmp run green (54/0). Cleanups CR-6/7/8 advisory.'

bug_resolution:
  bugs_fixed: 4
  bugs_remaining: 4
  fix_date: '2026-09-20'
  total_iterations: 1
  verification_method: 'Execution: symlinked CLI invocation, missing/dir script, NUL entry (both forms), recorded-run evaluator; suites 54/0; TMPDIR=/tmp 54/0'

recommendations:
  immediate:
    - action: 'Spawn the shell child with cwd: fixtureDir and add the no-cd eval fixture (BUG-5).'
      refs: ['shared/resources/security-probe.mjs', 'shared/resources/tests/security-probe.test.mjs']
    - action: 'Evaluator --git-base: derive commits/touched from git, fail closed on disagreement; Step 8a post-commit licence (BUG-6).'
      refs: ['shared/resources/finalise-fix-and-recheck.mjs', 'skills/finalise/SKILL.md']
    - action: 'Key the errored branch on bash launch-failure stderr (BUG-7); decline an expected that compares nothing (BUG-8).'
      refs: ['shared/resources/security-probe.mjs']
    - action: 'CR-4 tie the red marker to the named test; CR-5 carry shells in the record.'
      refs: ['shared/resources/finalise-fix-and-recheck.mjs', 'shared/resources/security-probe.mjs']
  future:
    - action: 'security-probe.mjs own CLI guard: silent no-op through .agents/skills (pre-existing, bug.4 class) — file as a general bug and apply the realpath compare.'
      refs: ['shared/resources/security-probe.mjs']
    - action: 'resolveEntry accepts an in-tree symlink pointing out of the tree (documented limit; the shell form then executes it) — consider realpath containment before spawn.'
      refs: ['shared/resources/security-probe.mjs']
    - action: 'Cycle-1 advisories CR-2 (severity rubric vs SEVERITY_BY_VERDICT), CR-4 (contract-test matcher), CR-5 (--emit-block default name); cycle-2 cleanups CR-6 (declined/escape ids lack @shell), CR-7 (hoist the stat check to runProbeSpec), CR-8 (duplicate refuses match).'
      refs: ['shared/resources/finalise-dod-security-prompt.md', 'shared/resources/tests/probe-boundary-signals.test.mjs', 'shared/resources/security-probe.mjs', 'shared/resources/probe-boundary-signals.mjs']
    - action: 'Observation #121 is parked; /finalise should set it actioned naming PR #446.'
      refs: ['docs/tasks/task.128.shell-boundary-probe-and-finalise-recheck/task.128.shell-boundary-probe-and-finalise-recheck.md']

deployment_readiness:
  staging: BLOCKED
  production: BLOCKED
  conditions: ['BUG-5 fixed and re-reviewed', 'BUG-6..8 fixed', 'CR-4, CR-5 fixed']

schema: 1
task: "task.77.review-pr-in-pipeline"
task_title: "Run the PR conformance review before a work item is finalised"
gate: CONCERNS
status_reason: "Cycle 4 acted on the operator's scope decision rather than attempting a fourth patch. The 5c resume predicate was DELETED and its mechanism replaced — completeness is now read from the implementation report's PR Review row, which is written every cycle by contract, removing the index arithmetic and the shell surface entirely. The ingester was corrected to describe the format /review-pr actually renders, and a test now pins the two files together. Both replacements are mutation-proved. CONCERNS rather than PASS because two items are deferred to filed follow-ups by design, not because anything is unresolved."
reviewer: "QA Engineer"
updated: "2026-09-03T00:00:00Z"

top_issues:
  - id: "TASK77-019"
    severity: high
    file: "shared/resources/develop-pipeline-resume-contract.md"
    finding: "zsh false-PASS in the resume verification command."
    suggested_action: "Resolved by deletion — there is no shell command in that cell any more. The check reads the implementation report's PR Review row, which the resume detector already reads for cycle reconstruction. Mutation-proved: restoring the index predicate turns the parity suite red."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-020"
    severity: high
    file: "shared/resources/develop-pipeline-resume-contract.md"
    finding: "gate.{N} and pr-review.{n} count different things, so the predicate was false on any run whose first gate was not clean."
    suggested_action: "Resolved by the same replacement — no indices are compared. docs/reference/pipeline-artifacts.md was corrected too: it now states explicitly that the two numbers must NOT be compared, and why."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-021"
    severity: high
    file: "shared/resources/develop-pipeline-resume-contract.md"
    finding: "Third strike on one predicate."
    suggested_action: "Honoured. The permitted move taken was REPLACE THE MECHANISM, on the operator's explicit decision. The rationale is recorded in the file itself so a future editor knows why the obvious filesystem check is deliberately not used."
    suggested_owner: po
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-022"
    severity: high
    file: "shared/resources/qa-findings-ingester-prompt.md"
    finding: "The ingester described a schema /review-pr never writes to disk."
    suggested_action: "Corrected to the real rendered three-line format, with the polymorphic `ref` called out and the absent `severity:` key forbidden by name. `pr-review` added to the source enum and to the severity ordering. A new test pins the header format across both files — they previously shared no assertion. Mutation-proved."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-023"
    severity: medium
    file: "shared/resources/develop-pipeline-step-5-6-qa-loop.md"
    finding: "The push-budget fix was prose that contradicted the numbered step an orchestrator executes."
    suggested_action: "Both the summary line and step 3 now carry the conditional arm, and step 5's PR-state poll is told what to expect on the unpushed path. The rule is restated as being about the push, not the commit."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-024"
    severity: medium
    file: "shared/resources/develop-pipeline-resume-contract.md"
    finding: "`review failed` was written by 5c and read by nothing."
    suggested_action: "Subsumed by the replacement. The resume table now enumerates every PR Review value with its action, so a non-terminal verdict cannot be missing from a negative match list — the failure shape of the old clause."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-025"
    severity: low
    file: "skills/qa-story/references/, skills/qa-task/references/"
    finding: "Partial re-bundle — four shared files reached only 4 of 6 consumers."
    suggested_action: "All six consumers refreshed and verified 6/6. Root cause diagnosed and filed as task 86: bundle_skill.py discovers references only from a skill's own files, so a transitively-bundled copy is never refreshed while the bundler reports 'in sync'."
    suggested_owner: dev
    status: closed
    fixed_date: "2026-09-03"
  - id: "TASK77-026"
    severity: medium
    file: "shared/resources/qa-findings-ingester-prompt.md"
    finding: "DEFERRED BY DESIGN, not unresolved. The REQUEST CHANGES path now parses a rendered text format by prose description. That is correct and pinned by a test, but it is two files agreeing about text rather than a structured contract. Filed as task 85."
    suggested_action: "Task 85 — emit a machine-readable findings block from /review-pr Step 7 and have the ingester prefer it. Kept out of this task because it changes the output of an accepted skill."
    suggested_owner: po
    status: open
  - id: "TASK77-027"
    severity: low
    file: "shared/resources/qa-execute-snippets.mjs"
    finding: "DEFERRED BY DESIGN. Commands in markdown table cells are invisible to the snippet-execution engine, which is why TASK77-019 survived three QA cycles. Filed as task 87."
    suggested_action: "Task 87 — extend extraction to table-cell commands. Out of scope here: it is a change to the QA engine, not to this task's subject."
    suggested_owner: po
    status: open

waiver:
  active: false

quality_score: 85

evidence:
  tests_reviewed: 327
  phases_verified: "7/7"
  trace:
    phases_covered: [1, 2, 3, 4, 5, 6, 7]
    phases_with_issues: []

nfr_validation:
  security:
    status: PASS
    notes: "Unchanged across four cycles."
  performance:
    status: PASS
    notes: "Unchanged."
  reliability:
    status: PASS
    notes: "The axis that failed cycles 1-3 is now sound. The resume check can no longer return a false PASS — it reads a value written every cycle by contract rather than deriving one from filenames, and every possible value has a stated action. The REQUEST CHANGES delivery path is described correctly and pinned. Both replacements are mutation-proved."
  maintainability:
    status: CONCERNS
    notes: "17 parity tests, none vacuous, and two of them now span file pairs that previously shared no assertion. The residual concern is structural rather than defective: the ingester contract is still prose-about-text (task 85), and the bundler can still silently stale a transitive copy (task 86). Both are filed with the diagnosis attached."

recommendations:
  immediate: []
  future:
    - action: "Task 85 — machine-readable findings block"
      refs: ["docs/tasks/task.85.review-pr-machine-readable-findings/"]
    - action: "Task 86 — bundler transitive refresh + CI freshness assertion (High: silent, and misreported as 'in sync')"
      refs: ["docs/tasks/task.86.bundle-transitive-refresh/"]
    - action: "Task 87 — execute table-cell commands"
      refs: ["docs/tasks/task.87.execute-table-cell-snippets/"]

deployment_readiness:
  staging: APPROVED
  production: CONDITIONAL
  conditions:
    - "Task 86 is High priority and independent of this change — a stale transitive bundle affects any skill, not only these."
